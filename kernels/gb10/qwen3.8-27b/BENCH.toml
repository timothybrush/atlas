# Benchmarks for this model, and the thresholds its gate records must meet.
#
# Sibling of MODEL.toml so the numbers sit beside the thing they describe;
# hardware and model are implied by the path and cannot disagree with the
# contents. Read by `avarok_plugin::gate::bench`.
#
# ★ This file may land BEFORE the model's MODEL.toml/kernels do (they arrive in
# a sibling change adding the qwen3.8-27b kernel target via a `kernel_source`
# redirect to qwen3.6-27b). `gate::bench::bench_files` discovers BENCH.toml by
# path alone for exactly this reason — the gate definition does not wait on the
# compiled target.
#
# Thresholds are per CHECKPOINT, not per model: two checkpoints of one model
# can differ by several BFCL points, so they cannot share a bar.
#
# This file is deliberately OUTSIDE the closure hash (`taxon::configs` does not
# list it), so ratcheting a threshold does not invalidate the record that
# justified the ratchet.
#
# `status = "unmeasured"` entries carry NO [benchmarks.metrics] table. Absence
# is the TODO -- a guessed threshold a run could clear would report PASS for
# something nobody measured.

[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.8-27B-NVFP4"
gate = "agentic-webserver"
recipe = "qwen3.8/qwen3.8-27b-nvfp4-unsloth"
label = "Qwen3.8-27B dense (NVFP4, unsloth)"
# NOT the default: the required Gate A subject stays the 35B MoE flagship
# (qwen3.6-35b-a3b/BENCH.toml), whose recorded history is untouched. This
# variant runs via `--checkpoint unsloth/Qwen3.8-27B-NVFP4`, or from the TUI's
# variant list.
default = false
status = "measured"
note = """\
The DENSE variant of the agentic gate. Not comparable to the 35B MoE entry in \
any direction: a dense 27B activates every parameter per token where the 35B \
MoE activates ~3B, so its wall band is roughly 2x — which is why this is a \
separate baseline entry with its own ceiling rather than a shared bar. \
REFERENCE RUN (the definition of these numbers): 2026-08-14, dgx2, binary main \
680b3a568, N=10 — webserver_ok 10/10, followed_directions 10/10, Sigma-wall \
1925.1 s, per-run walls 156.0/187.3/274.2/144.7/230.5/243.3/205.2/117.3/185.2/\
181.4 s (range 117-274 s, 2.3x min-to-max). That run FAILED only the \
35B-calibrated 1000 s budget while both correctness counts were perfect — the \
exact miscomparison per-variant thresholds exist to remove; under this entry's \
own ceiling it is a PASS. Serve profile of the reference run (what the recipe \
must pin): --gpu-memory-utilization 0.85 --max-batch-size 1 --max-seq-len \
32768 --kv-cache-dtype bf16 --lm-head-dtype bf16 --scheduling-policy slai \
--speculative --mtp-quantization bf16 --enable-prefix-caching \
--ssm-cache-slots 256 --ssm-checkpoint-interval 16 --kv-high-precision-layers \
auto, thinking ON. The 35B Gate-A profile (0.70 util, batch 2) CANNOT serve \
this model at all — it dies at boot with no memory left for KV cache — so the \
recipe is part of the variant, not a detail. The recipe id above now EXISTS in \
atlas-recipes (merged 2026-08-14 as atlas-recipes#19, commit 20ac6e8), \
pinning exactly the profile stated above, so this variant can self-start \
under --pull-request-gate. ★ Note the recipe pins `num_drafts: 3` (K=4), \
matching the reference run; the qwen3.8-27b target deliberately omits \
`default_num_drafts`, so an unpinned run would instead take the engine \
default K=2. When running against an operator-started endpoint pass \
`--param wall_budget_s=5000` explicitly -- 5000 is this entry's committed \
ceiling, NOT the 2500 an earlier revision of this note named: threshold_params \
substitution happens only under the gate (and in the TUI's variant step), so a \
plain `benchmark run` keeps the schema default 1000 s and a healthy dense tier \
(Sigma-wall ~1925 s) FAILS its run verdict on wall alone. \
★ SPEED IS NOT GATED ON THIS VARIANT. `s_per_turn_budget` defaults to 0.0 = \
non-gating and this entry commits no `[benchmarks.metrics.s_per_turn]` bound, \
because none has been measured here. That is deliberate: the 35B's measured \
8.5 s/turn is meaningless against the 18-40 s/turn documented below, and \
applying it would fail every dense run on a number drawn from another model. \
`s_per_turn`, `sum_agent_wall_s`, `sum_turns` and `decode_tps` ARE recorded on \
every run, so a bound can be ratcheted in from real tiers when they exist. \
The two 10/10 counts are ABSOLUTE correctness \
bounds and take no noise, same footing as the 35B entry; `iterations` is \
PINNED at exactly 10 for the same reason stated there — a count bound alone \
does not describe the run that produced it."""

[benchmarks.metrics.followed_directions]
min = 10.0

[benchmarks.metrics.iterations]
min = 10.0
max = 10.0

[benchmarks.metrics.sum_wall_s]
# Set from TWO measured N=10 tiers on dgx2 (2026-08-14, this branch's binary):
#   temp 1.0 -> 4090 s      temp 0.6 -> 4182 s
# Both 10/10 webserver_ok and 10/10 followed_directions, so the model is
# healthy at this wall; the number is what the current serve costs, not a
# defect. 5000 s is the slower tier + ~20% headroom.
#
# ★ This is ~2x the 1925 s reference tier taken 2026-08-14 on the pre-branch
# nightly. That gap is NOT sampling and NOT the inter-tool prose budget (both
# investigated and ruled out — see the MODEL.toml note and the prose-budget
# analysis): it is per-turn decode cost, 18 s/turn there vs ~40 s/turn here
# with turn counts unchanged. That regression is UNEXPLAINED and tracked
# separately; if it is found and fixed, this ceiling should come down with it
# rather than being left loose. A blow-up detector set at 5000 s only detects
# blow-ups relative to the CURRENT serve cost.
#
# Ratchet only after repeated back-to-back tiers, never to the best tier.
max = 5000.0

[benchmarks.metrics.webserver_ok]
min = 10.0

[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.8-27B-NVFP4"
gate = "bfcl-subset"
recipe = "qwen3.8/qwen3.8-27b-nvfp4-unsloth-bfcl"
label = "Qwen3.8-27B dense (NVFP4, unsloth)"
# THE REQUIRED bfcl-subset SUBJECT as of 2026-08-15 (flipped from Qwen3.6-27B per
# maintainer: 3.6 is no longer worth requiring on every PR). The 3.6 entry in
# kernels/gb10/qwen3.6-27b/BENCH.toml is now default = false — see the consequence
# note there re: the MLPerf submission no longer being gated.
default = true
# MEASURED 2026-08-14 (dgx1, this branch's binary, golden draw n=995 seed 42):
# overall 84.22 / normalized 84.12. See the metrics table below and the sharp
# caveat in the note.
status = "measured"
note = """\
The DENSE 3.8 leg of the BFCL gate. Exists so a Qwen3.8-27B score can be read \
directly against the recorded Qwen3.6-27B results — same golden ST-995 draw \
(62/10/10, floor 25, n=995, seed 42, temp 0), same scorer, and a serve recipe \
whose defaults are byte-identical to the 3.6 sibling's (atlas-recipes#20; only \
the checkpoint differs). That identity is load-bearing: BFCL's normalized \
score moves with serve config as well as with the draw, so the two legs must \
share a config or the generation-over-generation delta means nothing. \
Thresholds are ABSENT until a run establishes them; until then a run here \
BASELINES rather than gates. ★ Do NOT copy the 3.6 entry's floors \
(87.44 / 88.59) or the MLPerf floor (83.64 / 85.32) into this entry: same \
architecture and same draw, but DIFFERENT WEIGHTS, so neither transfers. The \
MLPerf-edge submission rides on 3.6, and nothing here guards it. \
★ One discontinuity vs the recorded 3.6 history: `num_drafts = 1` (K=2) was \
silently discarded before the 2026-08-14 --num-drafts precedence fix, so those \
3.6 numbers were measured at K=4. Speculative decode is lossless, so ACCURACY \
is comparable across that boundary; wall and TPOT are not. \
★ CERTIFIED BY SHARDS SINCE 2026-09-13 (gate::check_group): a complete partition \
of `--param shard=i/n` runs at one commit, any n — the campaign sizes n to its \
fleet (spark bench certify --shards) and the records say what they are \
(shard.index / shard.count; filed here as -s<i>of<n>). A whole-draw record under \
.benchmarks/bfcl-subset/ no longer satisfies this gate. ★ SCORED OPEN by owner decision: cross-request \
SSM snapshot reuse stays ON (no --hermetic pin here — contrast \
kat-equality-gate below, which pins the closed regime as its SUBJECT). The \
consequence is measured and named: 12 of the 995 answers differ between the \
whole draw and its four shards (#936, pin e897463b54) — \
live_irrelevance_2-0-2, live_irrelevance_8-0-8, live_irrelevance_14-2-2, \
live_irrelevance_15-2-3, live_irrelevance_16-2-4, live_irrelevance_40-2-28, \
live_irrelevance_47-2-35, live_irrelevance_55-2-43, live_irrelevance_71-2-59, \
live_irrelevance_79-2-67, live_multiple_57-22-4, live_parallel_multiple_3-2-1 \
(the list lives in benchmarks::bfcl::sensitive and every run reports \
known_partition_sensitive). The floors below are therefore cut from the \
SHARDED aggregate, never from a whole-draw run."""

[benchmarks.metrics.samples]
# The golden MLPerf draw is exactly 995. A run reporting anything else is not
# this draw and its score is not comparable — pin both ends. Under the shard
# rule check_group injects the UNION of the partition's shards here, so the pin
# still holds only when every index 0..n is present exactly once.
min = 995.0
max = 995.0

[benchmarks.metrics.overall_accuracy]
# SHARDED-OPEN FLOOR, provisional 2026-09-13. The whole-draw measurement was
# 84.22 (2026-08-14, dgx1) and its bar 83.82; the same draw as four shards on
# the shipped serve read 83.82 overall (#936, 2026-09-08). Bar = that sharded
# aggregate less the documented +-0.4 MTP-nondeterminism noise floor. To be
# CONFIRMED OR RE-CUT from the first certification campaign run under the
# shard rule (the stack that introduces it) — BENCH.toml is NON_COMPILED, so
# a re-cut does not void the records it is cut from.
# ★ RE-CUT 2026-09-15 (owner decision AZ, PR #1089): 83.42 -> 83.00. The
# shard COUNT is the campaign's to choose since #1089, and the number is
# partition-dependent (see the gate note above): the same draw, the same
# serve, the same post-#1071 code read 83.22 as four shards (c719406bf8,
# 2026-09-14) and 82.91 as six (48e7ee500e, 2026-09-15) — three hits of 995,
# in exactly the subsets the twelve known-sensitive samples live in
# (live_irrelevance -2, live_multiple -1, simple_python -1, parallel_multiple
# +1). Not a code regression (no engine path moved between the two) and not
# the reused server (the low shards ran on fresh ones). A floor cut at one n
# must hold at another, so the bar now carries the measured count effect:
# 83.00 with the 0.4 band puts the six-way reading 0.31 (~3 samples) above
# the line. It ratchets from here like every other floor.
min = 83.00
noise = 0.4

[benchmarks.metrics.normalized_single_turn_score]
# Measured 84.12, same run. ★ THIS IS 1.20 BELOW THE MLPERF-EDGE FLOOR (85.32).
# The floor was derived for Qwen3.6-27B and is NOT inherited here — different
# weights — so this is a QUALITY ALARM on this gate, not an MLPerf submission
# failure; the submission rides on 3.6, which still scores 87.44 / 88.59.
# ★ DO NOT treat this bar as an endorsement. Qwen3.8-27B scored 3-4 points
# BELOW Qwen3.6-27B on the same draw and serve config, and the per-subset split
# is suspicious rather than uniformly weaker: simple_python 95.16 against
# simple_java 45.16 and simple_javascript 29.03, with the hallucination category
# down 11 points (96.02 -> 84.85). A model does not lose 66 points between
# Python and JavaScript function calls by capability; that pattern points at a
# defect in the serve path, with the chat-template change the prime suspect
# (this run rendered through the checkpoint's own template, while every recorded
# 3.6 number came through the retired override, and the two serialize tool
# arguments differently). Tracked separately. These bars exist so a regression
# below TODAY's behaviour is caught; they are not a claim that today's behaviour
# is correct.
# SHARDED-OPEN FLOOR, provisional 2026-09-13: the whole-draw run read 84.12 and
# its bar was 83.72; the four-shard aggregate on the shipped serve read 83.36
# (#936) — normalized moves more than overall under partition because ten of
# the twelve flipping samples are live_irrelevance, which the normalized score
# weights heavily. Bar = 83.36 less the 0.4 noise floor; confirm or re-cut from
# the first sharded campaign, as above.
min = 82.96
noise = 0.4
# --- decode-floor ----------------------------------------------------------
# The pinned single-user decode-rate floor. REQUIRED
# (gate::coverage::REQUIRED) since 2026-08-15: the promotion ruling's
# precondition — a >=10-run sigma calibration on the gate's own instrument —
# is met (n=12, sigma ~0.05; see the note), and the bounds below are set from
# that calibration rather than from three points.
#
# The driver pins the whole request (MinHeap code prompt, temperature 0,
# seed 0, max_tokens 1500, runs = 3) and reports the MEDIAN server decode
# rate. Its vacuity pins (>=750 output tokens per run — the driver's
# MIN_OUTPUT_TOKENS, decode_floor/score.rs, which has been 750 since the gate
# landed and was never 800 as this line used to say; it sits under the
# instrument's deterministic 915-token natural stop — server rate present,
# accept_len_mean >= 1.5 from usage.completion_tokens_details.
# accepted_prediction_tokens) make a run that measured nothing INCONCLUSIVE
# rather than PASS — a thinking-on or non-speculating serve measures the
# serial floor, and that number must never be recorded as this gate's.

[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.8-27B-NVFP4"
gate = "decode-floor"
recipe = "qwen3.8/qwen3.8-27b-nvfp4-unsloth"
label = "Qwen3.8-27B dense (NVFP4, unsloth)"
default = true
status = "measured"
note = """\
BOX RULE (2026-09-13): this is a Speed-class gate; a campaign spread with \
`spark bench certify --with-nodes` measures it on another box ONLY when that \
box is equivalent by hardware::equivalence::SPEED_SPREAD (same GPU and driver \
line, clock ceiling within 1 %, memory within 5 %, no thermal throttle, \
chassis within 15 °C), decided from the records' own captures — otherwise \
every Speed unit is bundled on one node. Two GB10s otherwise identical read \
0.66 tok/s apart at 65 vs 89 °C, ten times this entry's sigma. \
CALIBRATED 2026-08-15, dgx1, this branch's binary: n=12 runs across 4 driver \
passes, every run 28.0-28.1 tok/s, mean 28.03, sigma ~0.05. Fingerprint (the \
instrument these bounds belong to): the DRIVER'S OWN MinHeap fixture, \
per-request reasoning_effort "none", temperature 0, seed 0, max_tokens 1500 \
with a deterministic 915-token natural stop, K4 num_drafts=3 (the recipe \
pins num_drafts: 3), accepted 569/915 per run, serve --max-batch-size 32 \
--gpu-memory-utilization 0.85 (the stock recipe serve). ★ SUPERSEDED BASIS, \
kept for the history: the previous floor (29.0, noise 1.0) came from an n=3 \
basis of 31.5/29.6/30.5 (median 30.5), 2026-08-15 — measured with a \
DIFFERENT instrument (batch-1 serve, an ad-hoc probe prompt, not the \
driver's fixture). Mechanism of the shift: different fixture + serve config \
moved the LEVEL by -2.4 tok/s while run-to-run sigma on the fixed instrument \
is 0.05 — an instrument change, not a regression. The gate is calibrated to \
ITS OWN instrument; never mix bases, and never ratchet from a best run. \
★ The gate serve needs NO operator flags for thinking: thinking-off arrives \
per-request (reasoning_effort "none" in the body — honored since the \
medium-default change), NOT from the recipe, so the stock recipe serve is \
the correct subject. ★ The accept pin depends on the accept-stats \
instrumentation (real accepted_prediction_tokens in usage); a binary \
without it reports INCONCLUSIVE by name, which is correct — a floor \
measured without proof of speculative engagement is the 2026-08-15 decode \
flip-flop all over again."""

[benchmarks.metrics.server_decode_tok_s]
# Median-of-3 server decode rate. The floor of 25.5 is the bound in force —
# an effective floor of 25.0 tok/s once noise 0.5 is applied by
# scoring::compare.
#
# ★ RATCHETED 2026-09-20, 25.0 -> 25.5 (effective 24.5 -> 25.0), to the
# owner's stated floor. Source record: .benchmarks/decode-floor/
# 2026-09-20-43a3c9b202.json (spark-28c2 = dgx3, 2026-09-20 12:06 UTC), which
# reads 26.16 tok/s. SPEED-BOUND POLICY (owner, 2026-09-20): a FLOOR is the
# latest measured value x 0.975 rounded DOWN to a clean step (0.5 below 100,
# 10 from 100 to 1000, 50 above 1000); a CEILING is measured x 1.05 rounded
# UP; "measured" is ONE named committed record, never a best run. 26.16 x
# 0.975 is 25.51, which rounds down to 25.5 — here the owner's stated value
# and the derivation agree. `noise` stays at 0.5 on purpose: it is the
# instrument's measured run-to-run band, not part of the margin, and every
# prior ratchet in this block kept it fixed so the ratchet shows in `min`.
#
# The set this floor is checked against, so it is not a one-draw floor:
# every committed record since the 2026-09-08 step, n=22 across five boxes —
#
#     spark-256a (dgx1)  n=4  25.91 26.02 26.11 25.98    <- slowest box
#     spark-43fa (dgx2)  n=8  26.09-26.26
#     spark-28c2 (dgx3)  n=8  26.15-26.27
#     edgexpert-aae5 / edgexpert-2640   n=1 each   26.13 / 26.04
#     all                n=22  mean 26.16  sigma 0.10  range 25.91-26.27
#
# The effective bar 25.0 sits 0.91 below the slowest committed run
# (2026-09-11-da446c920b, dgx1) and ~11 sigma below the mean, so it cannot
# fail a healthy run on any box in the set; a regression of 4.5% on dgx3 or
# 3.5% on dgx1 fails it. dgx1's four runs all sit below every dgx2/dgx3 run,
# which is why the margin is quoted against dgx1 and not against the mean.
#
# ★ RATCHETED 2026-09-11, 22.7 -> 25.0, from a fresh 12-run set on this
# entry's own instrument and on dgx2 (spark-43fa) — the SLOWEST box, which is
# where the superseded bar was drawn from too, so the basis is comparable:
#
#     26.3 26.1 26.1 26.2 26.1 26.1 26.1 26.1 26.1 26.1 26.1 26.1
#     n=12  mean 26.125  sigma 0.062  range 26.1-26.3
#
# sigma is quantization-limited (the gate reports one decimal), so the true
# spread is at most this. 24.5 sits ~26 sigma below the mean, and ~6.2% below
# it — wider than the mean-minus-5% the sweeps use, so this is a deliberately
# conservative ratchet rather than the tightest the data allows (that would be
# 25.3).
#
# ★ WHY THE LEVEL MOVED. It is a STEP, not drift: records from 2026-08-23 to
# 09-07 read 22.4-23.5 and records from 09-08 onward read 26.1-26.3, with
# accept_len_mean 2.6826 unchanged across the whole span — so this is a
# wall-clock change, not better speculation.
#
# ★ THE CAUSE IS NOT #968, AND AN EARLIER DRAFT OF THIS NOTE SAID IT WAS.
# Three things in this repository refute it, and they are recorded here so the
# wrong story is not re-derived:
#   * f9fd8ca43's own message states "C=1 IS IDENTICAL, AND THAT IS THE
#     DIAGNOSTIC — parent 18.5 / with bug 18.5". The bug it fixed only bites
#     when admissions interleave, so it cannot move a serial rung.
#   * The campaign measured AT f9fd8ca43 reads 22.58 here and C1 18.42 on the
#     sweep — both PRE-step values, at the very commit credited with the step.
#     That record was committed in ac736e938 and dropped in 3cfe4d8c45, which
#     is the only reason "every record from 09-08 reads 26.1-26.3" looked true.
#     `git show ac736e938:.benchmarks/decode-floor/2026-09-08-f9fd8ca431.json`.
#   * The DFlash2 ladder stepped on the same day, and that gate is
#     conflicts_with = "speculative", so an mtp-carry fix cannot reach it.
#
# The leading candidate is 7dd531f59 (#965, the GLM-5.3-Flash port), the only
# code change on the first-parent path through this window; it rewrote the
# dense_gemv M=1 sites that a 248K-vocab BF16 lm head leans on, which fits a
# gain that is large serially and vanishes above C=2. STATED AS A CANDIDATE,
# NOT A FINDING: no A/B across 7dd531f59^ has been run, and this note has
# already been wrong once about the cause.
#
# What the bar does NOT depend on: the step is measured, reproduced 15 times,
# and 24.5 sits far above the entire pre-step band whatever moved it. The
# attribution matters for knowing what to protect, not for whether to protect
# it.
#
# ★ RATCHETED 2026-09-06, 21.0 -> 22.7, and this is the FIRST time this
# entry's own ">=10-run set on this same instrument" rule has actually been
# satisfied. 21.0 was set in c9a682734f from n=3 (22.3 / 22.4 / 22.0) and sat
# ~1.3 tok/s under the band it was drawn from with no measured sigma behind
# the gap. The set behind 22.7 is ten consecutive gate runs — the gate's own
# serve, recipe as-is, bf16 lm head, no hand-built harness:
#
#     22.9 22.8 22.8 22.8 22.8 22.7 22.7 22.8 22.7 22.8
#     n=10  mean 22.78  sigma 0.063  range 22.7-22.9
#
# so 22.2 sits ~9 sigma below the mean. That is a real-regression margin, not
# a noise allowance.
#
# ★ THE 12-RUN 28.03 / sigma-0.05 CALIBRATION IS STILL NOT THIS NUMBER'S
# BASIS, and never was. For weeks the lines here advertised 27.0 as this
# gate's bar, derived as "mean minus ~1 tok/s", while the value beside them
# read 21.0 — so anyone reading the file to judge a measurement read a number
# 6 tok/s above the gate. (Phrased that way on purpose:
# scripts/check_bench_threshold_prose.py reads "the floor of N" as a claim
# about the bound in force, so a dead number must not be written in that
# shape. The live claim, at the top, is written in it deliberately and is
# checked.) That calibration ran on a hand-built serve with the DEFAULT lm
# head; the GATE serves the recipe, which pins lm_head_dtype bf16 as a
# correctness pin, and that head costs ~20%. Never re-derive from it.
#
# ★ WHICH BOX THE MARGIN IS MEASURED ON MATTERS, because the boxes differ by
# more than the within-box sigma. Same gate, same pin f3f77c82fc, same night:
#
#     dgx2 (spark-43fa)  n=10  mean 22.78   <- the calibration above, slowest
#     dgx3 (spark-28c2)  n=3   23.6 23.5 23.5
#     dgx1 (spark-256a)  n=1   23.7
#
# The 0.7-0.8 tok/s spread is between boxes, an order of magnitude above the
# 0.063 within-box sigma, so a floor drawn on one box does not describe
# another — which is exactly why ci.yml's "One PR, one commit, one signer"
# refuses a record set spanning boxes. 22.7 is drawn from the SLOWEST box on
# purpose. On dgx1, currently the only fingerprint in .github/record-signers/
# and therefore the only box that can certify, the margin is ~1.5 tok/s.
# Re-cut this floor if a second box is ever registered and is slower.
#
# noise 0.5 covers the run-to-run band (observed 0.2 peak-to-peak over the
# twelve runs) and stays under the 5% validate_noise cap (5% of 25.0 = 1.25).
# Ratchet only from a fresh >=10-run set on this same instrument.
min = 25.5
noise = 0.5
[benchmarks.metrics.output_tokens]
# The driver emits the MINIMUM completion_tokens across the 3 runs, so this
# floor means "every run decoded at least 750 of the 1500 budget" — the same
# vacuity pin the driver enforces (MIN_OUTPUT_TOKENS), restated where the
# gate can see it. 750, not 1200: the calibrated instrument's natural stop
# is a deterministic 915 tokens, and a pin above the instrument's own
# reference behaviour would fail every honest run.
min = 750.0

[benchmarks.metrics.runs]
# EXACT pin, like the BFCL draw size: a different run count is a different
# benchmark, and a median over a different n is not comparable to this floor.
min = 3.0
max = 3.0

# --- concurrency-sweep -----------------------------------------------------
# The latency/throughput ladder, REQUIRED (gate::coverage::REQUIRED) since
# 2026-08-15 — the maintainer wants this gate MANDATORY, and the thresholds-
# less-entry blocker is gone: this entry carries measured floors from the
# fixed instrument below. The qwen3.6-27b and qwen3.6-35b-a3b entries stay
# unmeasured declarations of intent; baseline_for drops unmeasured entries,
# so this is the gate's one default subject.
#
# ★ THE INSTRUMENT IS PINNED, because the schema defaults are a different
# instrument (C=1,2,4,8,16,32 at osl 128): the [benchmarks.param_overrides]
# table below is applied by gate self-start exactly as serve_overrides is on
# the serve side (bench_resolve::apply_param_overrides), recorded in the run
# record's params, and demanded on the record by check_record — a record
# measured on the schema default cannot read green against these floors.
# C=32 is deliberately NOT in the gate ladder: it needs preflight headroom
# not every recipe has, and the schema-default sweep still covers it for
# manual runs.
#
# Under the gate the driver self-verdicts against min_c1/min_c4/min_c8/
# min_c16/min_peak (auto-filled from the min bounds below); any vacuous cell
# or request error is a failing verdict regardless of the floors.

[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.8-27B-NVFP4"
gate = "concurrency-sweep"
recipe = "qwen3.8/qwen3.8-27b-nvfp4-throughput"
label = "Qwen3.8-27B dense (NVFP4, unsloth)"
default = true
status = "measured"
note = """\
BOX RULE (2026-09-13): Speed-class; spread across nodes only under \
hardware::equivalence::SPEED_SPREAD, else bundled — see the decode-floor \
note and docs/provable-benchmark-work.md §5c. \
★ INSTRUMENT RE-POINTED 2026-09-21 TO THE PUBLISHED LADDER'S, owner's call. \
This gate now measures ISL 128 / OSL 1024, ctx 2048, batch 128, fp8 KV, \
ESSAY fixture — the instrument bench/ladder38 measured for the published \
Atlas-vs-vLLM ladder, axis for axis. It previously measured ISL 512 / OSL \
320, ctx 4096, natural fixture. site/src/lib/ladder-baselines.js exists \
because those two instruments were being read as one engine (~478 vs ~116 \
tok/s on the same checkpoint, same day); the fix asked for is not a chart \
change but this one — the site's top chart must show the LIVE Atlas against \
the measured vLLM bar, and that requires the live gate record to fingerprint \
as the published instrument on every REQUIRED axis (isl, osl, prompt_mode, \
max_model_len, max_batch_size, kv_cache_dtype). \
★ RE-CUT 2026-09-23: the nine rung floors and min_completion_tokens now carry \
bars cut from the first gate record on this instrument plus the same-instrument \
ladder38 history on all three boxes -- see the derivation block directly \
above [benchmarks.metrics.c1_aggregate_tok_s]. The paragraph that follows is \
the 2026-09-21 state, kept so the sequence stays legible. \
★ EVERY RUNG FLOOR IS THEREFORE OFF (min = 0.0), NOT LOWERED. The committed \
floors were cut at the retired instrument and sit 1.15x to 4.3x below what \
this one produces — published Atlas on it reads C1 23.59, C2 41.02, C4 \
74.21, C8 125.95, C16 203.36, C32 291.01, C64 386.63, C128 478.11 — so \
leaving them in place would report a guard that cannot fail anywhere above \
C=2 while risking a false FAIL at C=1, where 20.5 sits 13% under. `RUNGS` in \
concurrency.rs names 0.0 as the record-without-gating state and that is \
exactly what it is for: measure, then declare. The sweep is NOT unguarded \
meanwhile — vacuous_cells = 0 and min_completion_tokens keep their bars, and \
a sweep with any request error never passes, whatever the floors say. Re-cut \
all nine together from >=3 records on THIS instrument under the speed-bound \
policy and its guard, in a follow-up PR. \
★ THIS RE-POINT INVALIDATES EVERY CONCURRENCY-SWEEP RECORD COMMITTED BEFORE \
IT, because check_record demands an exact serve-pin and param match. That is \
correct; they measured a different instrument. \
★ NOTHING IS STILL UNPINNED, as of 2026-09-22: this entry's `recipe =` \
line now names qwen3.8/qwen3.8-27b-nvfp4-throughput, and that recipe IS the \
published leg's CLI frozen as a file -- verified key for key against \
published.json `series[0].cli`, including the two the gate had been getting \
wrong by inheritance (--ssm-cache-slots 8, and NO --lm-head-dtype, i.e. the \
native NVFP4 head). serve_overrides therefore drops from seventeen pins to \
three, and the three that remain are there for the site fingerprint, not for \
the serve -- see the block below. Three of the four env-only levers it \
also carried cannot be pinned there (no ServeArgs flag exists for FP8_ROWWISE, \
MTP_DCUT_RATIO or MTP_K_LADDER; PREFILL_CODISPATCH became --prefill-codispatch \
on 2026-09-22 and IS pinned below). Until #1242 they were set in each node's \
bench.yaml env: map, the only input the agent's env_clear() passes through -- \
and which is NODE-WIDE, so arming them re-tuned every other gate on that node \
too: campaign 8 attempt 7 failed ttft-warm-gate at +5.4%% median with \
AVAROK_PREFILL_CODISPATCH=1, against three control records at 0 that agree to \
0.045%%, and bfcl-subset at util 0.70 could not boot under the FP8 twins. \
They are pinned PER GATE in [benchmarks.serve_env] below since #1242: the \
harness applies exactly that set to the serve it starts and refuses, by name, \
any AVAROK_* lever its own environment carries that the recipe or this table \
did not declare -- so a node's bench.yaml env: must be toolchain-only. \
Speculation IS shared — the recipe pins --speculative --mtp-quantization \
bf16 with num_drafts 3, the same drafter and K the published leg ran — so \
the live leg is not a weakened Atlas against a speculating vLLM. The \
remainder is the engine under test, which is the point of a live series: \
the WORKLOAD is pinned here, the configuration is whatever main ships. \
★ READ FIRST — EVERY LADDER FIGURE IN THIS NOTE IS SUPERSEDED HISTORY, NOT \
THE BOUNDS IN FORCE. The floors this entry enforces were re-cut 2026-08-30 \
from the WIDENED instrument (C=1..128 at bs=128, concurrent warm-up); their \
derivation is the block comment directly above \
[benchmarks.metrics.c1_aggregate_tok_s] and the numbers are the metric tables \
themselves. The 2026-08-15 ladder described below is a DIFFERENT instrument \
(C=1/4/8/16 at bs=32) and its five floors no longer exist. Kept because the \
osl=320 calibration and the serve-profile reasoning still hold. || \
MEASURED 2026-08-15, dgx1, this branch's binary: 3 reps of the full ladder \
on the FIXED instrument — C=1/4/8/16, isl 512, osl 320, natural (MinHeap) \
code fixture, per-request reasoning_effort "none", temperature 0, seed 0, \
warmup 1 — zero vacuous cells and zero request errors in every rep. \
Aggregate tok/s (incl. TTFT), per C: C1 25.6/25.5/25.5 (mean 25.5, sigma \
0.05); C4 45.6/48.7/48.3 (mean 47.5, sigma 1.4); C8 66.7/69.0/67.1 (mean \
67.6, sigma 1.0); C16 98.3/99.2/99.1 (mean 98.9, sigma 0.4). TTFT p50 \
~1.3/4.5/8.4/15.9 s at C=1/4/8/16. ★ WHY osl=320: the natural fixture's \
minimum deterministic completion WAS 334 tokens when this was cut — it is 296 \
today, after the serial-in-think fix (2c80bf2f7); see min_completion_tokens \
below, and note the 320 budget still binds — so a 320-token budget makes \
EVERY request hit the budget ("length" finish) and every cell comparable — \
at osl=512 the C=16 cell was ALWAYS vacuity-flagged, deterministically, \
i.e. the default budget cannot measure the top rung on this model. That is \
the vacuity guarantee min_completion_tokens pins below. Floors are set at \
mean minus max(3*sigma, ~5%) per rung, so a sub-noise wobble passes and a \
real regression does not. Recalibrate all five together, from >=3 fresh \
reps on this same instrument, never from a best rep. \
★ SERVE PROFILE (2026-08-15): the recipe is the shared AGENTIC profile — a \
serial reproduction config (batch 1, bf16 KV, 256 Marconi slots, 32K \
context) that strangles a concurrency instrument: the first certified run \
served it verbatim and measured C=1..16 all ~19 tok/s with TTFT stacking \
linearly, and its serve sized only ~0.6-1.1 GB of KV — nowhere near 16 \
concurrent ~832-token sequences. That reasoning stands, but its REMEDY was replaced on 2026-09-22: rather than \
override the agentic recipe key by key, the entry now points at the \
throughput recipe, which is the published leg's own profile. The \
lm_head_dtype sentence that stood here -- "stays the recipe's bf16 \
correctness pin" -- was wrong and is retracted; see the serve_overrides \
block. \
★ THOSE FIVE FLOORS ARE GONE — RE-CUT 2026-08-30; this paragraph is kept so \
the sequence stays legible, and it described the file's state for weeks after \
it stopped being true. The five placeholders were 24/43/63/94/94, cut from \
the 2026-08-15 hand-built batched calibration serve rather than from a \
certified run on the pinned profile (fp8 KV, 8 Marconi slots, 4K context \
change the memory geometry). They were replaced by the EIGHT floors now in \
the metric tables (C=1..128), cut together from 3 fresh reps of the widened \
bs=128 instrument — see the block comment above the first metric table for \
their derivation, and for the stated fact that the mid-ladder rungs are \
loose. \
★ WARM-UP SEMANTICS CHANGED: historical records sent one unrelated `warm` \
prompt per cell, then measured `c0..cN`; later rungs also reused prompts from \
earlier rungs. `warmup = 1` now runs every exact measured prompt once before \
timing. The floors quoted above predate that controlled-cache instrument and \
could not calibrate it — and the replacement HAPPENED: the 2026-08-30 re-cut \
ran on the concurrent-warm-up instrument, and the metric tables below are its \
output."""

# ★ PLACEMENT: a [benchmarks.*] sub-table attaches to the MOST RECENT
# [[benchmarks]] header above it. This serve pin was first committed ABOVE
# this entry's header (2026-08-15), so TOML silently attached it to the
# PREVIOUS entry (decode-floor) and the gate served the recipe's batch 1
# with no OVERRIDES disclosure line — keep every sub-table BELOW the header
# it belongs to. bench_tests pins where each committed serve pin sits.
[benchmarks.serve_overrides]
# ★ SEVENTEEN PINS -> THREE, 2026-09-22, owner's call: "the gate serves the
# throughput recipe ... match the throughput recipe". The `recipe =` line above
# now names qwen3.8/qwen3.8-27b-nvfp4-throughput, and that file is not a
# similar profile -- it is the published ladder's CLI frozen, verified key for
# key against bench/ladder38/published.json `series[0].cli`. So the fourteen
# pins removed here were re-stating the recipe's own defaults back at it:
#   max_batch_size 128, kv_cache_dtype fp8, max_model_len 2048, mtp_gate force,
#   num_drafts 3, mtp_quantization bf16, speculative true, scheduling_policy
#   fifo, ssm_checkpoint_interval 32, ssm_h_dtype f16-pool, gdn_fused_norm
#   true, ssm_batched_recurrent true, prefill_varlen_batch true,
#   ssm_tail_midchunk false, disable_thinking true, request_timeout 0.
# Each was compared to the recipe's value before removal. The f16-pool /
# gdn_fused_norm boot requirement (serve_phases/preflight/ssm_h_fp16.rs bails
# without the pair) is still satisfied -- by the recipe, which sets both -- and
# the pair still must not be split.
#
# ★ WHAT WAS ACTUALLY WRONG. The gate had been serving
# qwen3.8/qwen3.8-27b-nvfp4-unsloth -- the AGENTIC profile -- and overriding it
# back toward the published leg one key at a time. Two of that recipe's
# defaults were never in the override list and so were inherited in silence:
# `lm_head_dtype: bf16` and `kv_high_precision_layers: auto`. The bf16 head is
# the expensive one. kernels/gb10/HARDWARE.toml sets lm_head_batchm_max = 8, so
# above M=8 lm_head_batched.rs abandons the batched GEMV and falls to
# dense_gemm_bf16.cu: 16x16 tiles, one thread per output element, a scalar
# accumulate over K=5120 into N=248320. That is ~3.25e11 FLOP per decode step
# at M=128 -- paid once per step, i.e. a PER-SEQUENCE cost that grows with the
# rung. It is exactly the shape of the measured deficit (0.91x at C=1
# degrading to 0.51x at C=32); a flat regression cannot make that curve.
#
# A/B on this box, one sha, one harness, C=8/32/128 (scripts/ab_sweep.sh, a
# SHORT-OSL RANKING RIG at osl 256 -- these are ratios, not ladder figures;
# this gate's own osl is 1024 and the published bar is quoted there):
#
#     C=8     74.5 -> 104.9 tok/s  (+41%)   TPOT  94.8 -> 68.6 ms
#     C=32   136.8 -> 198.9 tok/s  (+45%)   TPOT 206.2 -> 134.5 ms
#     C=128  198.6 -> 303.6 tok/s  (+53%)   TPOT 550.9 -> 327.2 ms
#
# The gain growing with width is the per-sequence signature, as predicted, and
# the treatment leg's serve log carries `LM head quantized to NVFP4` while the
# control's does not -- the lever is proved armed, not assumed.
#
# ★ THE THREE BELOW ARE NOT REDUNDANT, THOUGH THEY DO RESTATE RECIPE DEFAULTS.
# site/src/lib/ladder-baselines.js builds a record's fingerprint from `params`
# + `serve_overrides` and NOTHING ELSE (line 88; it never reads
# serve_resolved), and all three are REQUIRED_AXES. Dropped from this table
# they would read null, null counts as a difference on a required axis, and the
# live series would stop being comparable to published.json -- the precise
# defect the 2026-09-21 re-point existed to fix. The other three required axes
# (isl, osl, prompt_mode) come from [benchmarks.param_overrides] below.
max_batch_size = "128"
kv_cache_dtype = "fp8"
max_model_len = "2048"
# ★ THE FOURTH PIN IS A SERVE PIN, NOT A FINGERPRINT PIN, and it is the one
# lever the published leg carried that the recipe cannot: AVAROK_PREFILL_CODISPATCH
# was env-only and node-wide, so it could not be given to this gate without also
# giving it to ttft-warm-gate (+4.78% warm TTFT, 107x the control spread, over a
# +3.0% limit). --prefill-codispatch makes it per-gate. The dense proof that beats
# vLLM at every rung (proof/MANIFEST.json, 2026-09-22) was measured WITH it; a
# certification without it measures a different, slower engine than the site
# claims. Requires the flag to exist in the tree being certified.
prefill_codispatch = "true"
# ★ ssm_cache_slots IS DELIBERATELY ABSENT: 32 -> the recipe's 8, which is also
# `--ssm-cache-slots 8` on the published CLI, so this table's last disagreement
# with that leg is gone. The measured case for 32 above was real but
# INSTRUMENT-SPECIFIC, and its instrument was retired on 2026-09-21: the
# slots_needed calibration was taken on isl 512 / natural-fixture ~832-token
# prompts, where a Marconi snapshot restore is eligible at all. This gate now
# sends isl 128, which renders to ~200 tokens -- under
# DEFAULT_MARCONI_MIN_TOKENS = 256 (mtp_carry.rs) -- so every restore is
# DECLINED by design and the pool size barely moves warm behaviour. 8 slots
# reserve 1.18 GiB against 32's 4.73 GiB, returning ~3.55 GiB to the KV budget,
# which is where the widest rungs need it.
# Consequence, stated rather than discovered: at 8 slots only C=1 is
# `warm_cache_capable` (slots_needed is 3/8/18/38 at C=1/2/4/8 and the rule is
# a strict `>`), so C>=2 measures cold BY CONSTRUCTION. That does not fail
# anything -- cache_is_uncontrolled is evaluated per cell and only when
# warm_capable (concurrency.rs:747), so those cells skip the warm rule rather
# than trip it.
# ★ lm_head_dtype IS ALSO ABSENT, and that is the whole point: the throughput
# recipe leaves it unset, so the head runs the checkpoint's native NVFP4. The
# comment that stood here called the agentic recipe's bf16 head "a correctness
# pin (exact argmax over the 248K vocab), not a throughput knob". Both halves
# were wrong: nothing ever measured an argmax difference, the published 478.11
# ladder itself ran with no --lm-head-dtype flag, and the "not a throughput
# knob" claim was off by 41-53%.

[benchmarks.serve_env]
# The AVAROK_* levers the ladder is measured under — the published dense
# concurrency recipe (2026-09-22). Not recipe keys, so serve_overrides cannot
# carry them; until #1242 they reached the serve only as the node agent's
# bench.yaml `env:`, which handed them to EVERY gate on the box (bfcl-subset
# at util 0.70 cannot boot with the +23.4 GB of FP8 twins) and disclosed them
# on no record. Pinned HERE rather than in the recipe's `env:` because the
# recipe (qwen3.8/qwen3.8-27b-nvfp4-throughput, the published ladder's CLI
# frozen) lives in the atlas-recipes index, not in this tree, and a gate pin
# must land with the gate's own floors; the harness merges the recipe's
# `env:` (empty for every vendored recipe) under this table, pin wins.
# Applied by gate self-start exactly as the tables above are, fingerprinted
# into the leased server's identity (`env_sha256`), and disclosed on the
# record as `serve_env` (disclosure only — check_record does not demand it,
# so the records measured under the bench.yaml export stand).
AVAROK_FP8_ROWWISE = "1"
AVAROK_MTP_DCUT_RATIO = "1.0"
AVAROK_MTP_K_LADDER = "1:3,2:1,4:2,8:2,16:1"

[benchmarks.param_overrides]
# The gate's instrument — see the block comment above. Values are parsed by
# each parameter's own schema kind, exactly like --param KEY=VALUE.
# 1..128 as of 2026-08-29: the published Atlas-vs-vLLM ladder quotes C=1..128,
# and a certification that stopped at 16 left the four widest rungs of the
# claim ungated. C=2 is in because it is the thinnest margin on that ladder
# and the one most easily lost silently.
concurrencies = "1,2,4,8,16,32,64,128"
# ★ 512/320 natural -> 128/1024 essay on 2026-09-21. These three lines ARE
# the re-point; everything else in this entry follows from them.
#   isls        128    the published ladder's ISL (~200 rendered tokens)
#   osl        1024    its output budget
#   prompt_mode essay  `Fixture::Essay` in concurrency.rs, whose ESSAY_TASK is
#                      SUFFIX_ESSAY from bench/ladder38/harness_w55_conc_ladder.py
#                      BYTE FOR BYTE (nonce width 6, presence_penalty and
#                      frequency_penalty pinned to 0.0 on this fixture only),
#                      SHA-256-pinned against the Python harness by
#                      concurrency_verdict_tests.
# ★ WHY THE FIXTURE HAD TO CHANGE WITH THE BUDGET, and why this is still only
# a parameter change: the natural (MinHeap code) fixture's deterministic
# completion is ~296 tokens, so at osl 1024 every cell would be vacuous by
# construction — occupancy 0.29 against a 0.8 floor — and the gate would be
# INCONCLUSIVE forever. The essay fixture is the published instrument's own
# request and elicits the full budget. It already exists in the driver, so no
# kernel or stats path moves and this entry costs no campaign of its own;
# BENCH.toml is in NON_COMPILED_KERNEL_FILES.
isls = "128"
osl = "1024"
prompt_mode = "essay"

# ── Floors re-cut 2026-08-30 from 3 fresh reps of the WIDENED instrument
# (C=1..128, bs=128, concurrent warm-up), mean - max(3*sigma, ~5%) per rung,
# every rung cut together. The previous five were cut on a hand-built bs=32
# calibration serve and describe an instrument that no longer exists.
#
# ★ THE MID-LADDER FLOORS ARE LOOSE, AND THAT IS HONEST RATHER THAN LAZY.
# C=8's sigma is 4.21 across three back-to-back reps on one frozen binary, so
# 3*sigma is 12.6 and the floor lands 21% under the mean; C=4 is 16% under.
# The top four rungs are 20-40x tighter and their 5% arm governs. A gate that
# only fires below 48 when the engine normally does 60 will not catch a 15%
# regression at that rung -- but a tighter bar on this variance fails on noise,
# and a gate that cries wolf gets switched off. The mid-ladder variance is the
# same phenomenon avarok#812 is chasing; when that is root-caused these two can
# be re-cut much tighter. Stated here so nobody reads C=8 as well-guarded.

# ── RE-CUT 2026-09-20 under the SPEED-BOUND POLICY (owner, 2026-09-20): a
# FLOOR is the latest measured value x 0.975 rounded DOWN to a clean step (0.5
# below 100, 10 from 100 to 1000, 50 above 1000); a CEILING is measured x 1.05
# rounded UP. "Measured" is ONE named committed record, never a best run: here
# .benchmarks/concurrency-sweep/2026-09-20-43a3c9b202.json (spark-28c2 = dgx3,
# 2026-09-20 11:36 UTC, on the current 32-slot pin), which reads
#
#     C1 21.21  C2 31.66  C4 53.17  C8 64.73  C16 91.76
#     C32 104.99  C64 116.38  C128 116.26  peak 116.38 (at C=64)
#
# ★ THE GUARD, applied to every rung before its policy floor is written: the
# effective bar (min - noise) must not exceed ANY committed PASS value of the
# rung since the 2026-09-08 step (the post-#968 regime: n=22 across
# spark-256a/dgx1, spark-43fa/dgx2, spark-28c2/dgx3, edgexpert-aae5 and
# edgexpert-2640). A floor that would have failed a committed healthy run is a
# floor cut from a biased draw — the trap already met twice at C=2, where 26.0
# and then 25.2 were each proposed from a biased set and each was wrong — so
# where the policy floor fails the guard, the rung is cut at the regime
# MINIMUM x 0.975 rounded down instead, and if even that does not exceed the
# old floor, the old floor stands. The regime deliberately includes the 8-slot
# records of 2026-09-08..13 (the pin moved to 32 slots at
# 2026-09-14-1a9edba35d): dgx1, the slowest box, has NO record on the 32-slot
# pin yet, and it is the box a floor is most likely to fail on.
#
#     rung  latest  policy  regime min (record, box)          written  basis
#     C1    21.21   20.5    20.95 (09-13-93f380f24b, dgx1)    20.5     policy
#     C2    31.66   30.5    26.74 (09-19-18d809f1ab, dgx2)    25.0     KEPT, see block
#     C4    53.17   51.5    44.66 (09-12-8b33b70627, dgx1)    43.5     regime min
#     C8    64.73   63.0    58.83 (09-11-da446c920b, dgx1)    57.0     regime min
#     C16   91.76   89.5    88.61 (09-18-0719dea47d, dgx3)    86.0     regime min
#     C32   104.99  100     103.60 (09-13-1a0dc88a8c, dgx1)   100      policy
#     C64   116.38  110     114.57 (09-11-da446c920b, dgx1)   110      policy
#     C128  116.26  110     114.00 (09-11-da446c920b, dgx1)   110      policy
#     peak  116.38  110     114.57 (09-11-da446c920b, dgx1)   110      policy
#
# Five rungs and the peak take the policy floor; the three that do not are the
# ones whose spread is wider than the policy's 2.5% margin (C4 sigma 3.05, C8
# 2.96, C16 1.39 on n=22, each multimodal by box and by warm/cold cell state)
# and each says so in its own block. The 10-step above 100 makes C32 land 4.8%
# under its latest value rather than 2.5%; that is the policy's rounding, not
# a judgement. `noise` is untouched on every rung, as the 2026-09-11 ratchet
# left it, so the ratchet is visible in `min` and not hidden in the allowance.

# ── RE-CUT 2026-09-23, THE FIRST CUT ON THE 128/1024 ESSAY INSTRUMENT. Policy
# as on 2026-09-20 (owner): a FLOOR is the latest measured value x 0.975
# rounded DOWN to a clean step (0.5 below 100, 10 from 100 to 1000, 50 above),
# "latest" being ONE named committed record:
# .benchmarks/concurrency-sweep/2026-09-23-6c75c09da4.json (spark-28c2 = dgx3,
# 2026-09-23 00:28 UTC, PASS, the only gate record on this instrument), which
# reads
#
#     C1 22.78  C2 41.47  C4 73.58  C8 125.46  C16 199.82
#     C32 285.74  C64 385.89  C128 478.91  peak 478.91 (at C=128)
#
# ★ ONE GATE RECORD IS NOT A REGIME. recut_floors.py exits 3 on this directory
# ("fewer than three records on this instrument"), and its pure policy bars
# (22.0 / 40.0 / 71.5 / 120 / 190 / 270 / 370 / 460) would fail a healthy dgx1
# run at C=2, C=4, C=8 and C=128 outright: the 2026-09-22 dgx1 proof reps read
# 39.02 / 69.05 / 119.84 / 459.32. So the GUARD is applied against the whole
# same-instrument history instead. The regime is every clean run of the
# published build or later, on the ladder38 harness this gate's ESSAY fixture
# reproduces byte for byte (the gate takes ONE timed rep per cell, so the
# harness's per-rep values -- not its 3-rep means -- are the right noise
# model): bench/ladder38 rounds 10 and 11 (dgx2, 2026-08-17; round 11 is the
# published series), the 2026-08-18 C=2/C=8 hardening runs (dgx2), the
# 2026-08-20 stack-tip sweeps on dgx1 (idle), dgx2 and dgx3 plus the dgx1
# C=64/C=128 cross-box run, the 2026-09-22 dense proof on dgx1 (x2), dgx2 and
# dgx3 (proof/MANIFEST.json, tree 8435a65348), and the gate record. n per rung
# is 13-31 reps across three boxes. EXCLUDED, with the reason written down:
# sweep_atlas_stacktip_dgx1_20260820.json, which RESULTS.md (the "dgx1
# CONTENDED | dgx1 IDLE" table) records as measured minutes after the box had
# served another session, and which was re-run idle for exactly that reason.
# Rounds 4-9 are pre-K-ladder and read a different engine (C8 82-111), so they
# are outside the regime the way pre-#968 records were outside the 09-20 one.
#
# ★ THE GUARD, read strictly because the regime is harness reps rather than
# gate records: the written bar must clear EVERY regime rep by at least the
# policy's own 2.5%, i.e. floor <= regime-min x 0.975 rounded down. Where the
# policy bar meets that, it is written; where it does not (it is within 2.5%
# of, or above, the slowest box's slowest rep), the regime-minimum basis is
# written instead. Every regime minimum is a dgx1 rep except C=1, where the
# gate record itself is the minimum.
#
#     rung  latest  policy  regime min (rep, box)               n   written  basis
#     C1    22.78   22.0    22.78 (gate 6c75c09da4, dgx3)       19  22.0     policy
#     C2    41.47   40.0    39.02 (proof 09-22, dgx1)           22  38.0     regime min (policy fails outright)
#     C4    73.58   71.5    69.05 (proof 09-22 resample, dgx1)  28  67.0     regime min (policy fails outright)
#     C8    125.46  120     119.84 (proof 09-22, dgx1)          31  110      regime min (policy fails outright)
#     C16   199.82  190     192.22 (stack tip idle 08-20, dgx1) 28  180      regime min (policy clears by 1.2% only)
#     C32   285.74  270     274.68 (stack tip idle 08-20, dgx1) 28  260      regime min (policy clears by 1.7% only)
#     C64   385.89  370     375.08 (cross-box 08-20, dgx1)      13  360      regime min (policy clears by 1.4% only)
#     C128  478.91  460     459.32 (cross-box 08-20, dgx1)      13  440      regime min (policy fails outright)
#     peak  478.91  460     459.32 (same run; peak is C=128)    13  440      = C128, see the peak block
#
# The written bars sit 3.5% (C1) to 8.2% (C8, the 10-step rounding) under the
# slowest clean rep ever measured on this instrument, and 3.4% (C1) to 12.3%
# (C8) under the gate record. They are BELOW the published vLLM+MTP bars at
# C=4/8/16/32 (71.61 / 124.48 / 197.03 / 283.48) by construction: Atlas's
# margin there is 1-3% and a floor at the bar would fail a healthy run one
# time in several -- the gate guards Atlas against ITSELF; the site's chart is
# where the vLLM comparison is read. `noise` stays 0.0 on every rung so the
# bar is the number written. RE-CUT under the pure policy (latest x 0.975,
# guard over committed PASS records) once >= 3 gate records exist here; the
# thin-margin rungs (C16/C32/C64) are the ones that will tighten first.

[benchmarks.metrics.c1_aggregate_tok_s]
# ★ RE-CUT 2026-09-23, 11.5 -> 22.0, on the 128/1024 essay instrument (derivation
# block above the C1 table): latest 22.78 x 0.975 = 22.21 -> 22.0 (policy). Regime min is the gate
# record itself (22.78; dgx1 22.90 on the 2026-09-22 proof, dgx2 23.19 on
# round 10; n=19), 22.0 clears it by 3.5%.
#
# ★ RATCHETED 2026-09-20, 20.4 -> 20.5 (effective 20.1 -> 20.2 after the
# unchanged `noise` of 0.3): 20.5 was the policy floor on the
# 2026-09-20-43a3c9b202 value 21.21 (x 0.975 = 20.68, rounded down). Guard:
# regime min 20.95 (2026-09-13-93f380f24b, dgx1; n=22, sigma 0.11), 0.75
# above the effective bar. The smallest ratchet in this re-cut, because the
# 2026-09-11 one below already sat at the mean minus 5%.
#
# ★ RATCHETED 2026-09-11, 17.5 -> 20.4 (effective 20.1 after the 0.3 allowance). This
# rung is cut ALONE, which the block above says the ladder is not — "every rung
# cut together". The exception is deliberate and the reason is that only this
# rung's instrument moved:
#
#     C=1  pre-#968  n=47  18.2-20.1      post-#968  n=14  21.2-21.3
#
# f9fd8ca43 ("mtp-carry: give the store ticket its own dispenser — sharing the
# capture counter cost 24% of C=2 decode", #968) stepped the single-stream
# rungs and left the batched ones where they were. decode-floor records the
# same step on the same day. Cutting the whole ladder from one post-step
# calibration would move seven rungs whose LEVEL did not change, on three reps
# fewer than the 2026-08-30 cut used.
#
# The set behind 20.4 — ten consecutive gate runs on this same instrument,
# dgx2 (spark-43fa), plus the four post-step records already committed:
#
#     21.2 21.2 21.2 21.2 21.2 21.2 21.2 21.2 21.3 21.2
#     n=10  mean 21.21  sigma 0.030  range 21.2-21.3
#
# mean - max(3*sigma, 5%) is 20.15, the 5% arm governing exactly as it does for
# the top four rungs; 20.4/0.3 lands there while leaving `noise` untouched so
# the ratchet is visible in the bound rather than hidden in the allowance.
#
# ★ WHY THE OTHER SEVEN RUNGS ARE NOT CUT HERE, measured rather than assumed,
# from the same ten runs:
#
#   C2   mean 29.75 sigma 1.470 — trimodal; run 10 of this very set read 25.5
#        after nine in 29.9-31.3. The formula bar (25.34) clears that by 0.6%.
#        The floor belongs in the gap between modes, where it already is.
#   C4   mean 49.56 sigma 1.495 — formula bar 45.07 against a current-regime
#        minimum of 45.4. 0.7% is not a margin.
#   C8   mean 63.05 sigma 1.609 — the block above defers this rung explicitly
#        until avarok#812 is root-caused, and nothing here root-caused it.
#   C16  mean 89.95 sigma 0.206 — looks tight in a fresh set, but its own
#        history dips to 82.4 with NO regime change, so the formula bar (85.45)
#        would have failed a run that was healthy. This is the rung that proves
#        a ten-run set is not a substitute for reading the history.
#   C32/C64/C128  all clear their formula bars by ~4.5%, but each has only
#        n=11 committed history and none stepped. ~2.5 tok/s of tightening is
#        not worth spending that thin a record on; re-cut them with the ladder.
#
# ★ PROVISIONAL, 2026-09-21. THE BAR ABOVE WAS CUT AT THE RETIRED 512/320
# NATURAL INSTRUMENT AND IS NOT A BOUND ON THIS ONE — it sat 1.15x (C=1) to
# 4.3x (C=128) under what this instrument produces, so carrying it would have
# reported a guard that cannot fail while risking a false FAIL at C=1.
#
# ★ AND IT COULD NOT SIMPLY BE SWITCHED OFF. `RUNGS` says a rung with no
# metrics block keeps the 0.0 default and gates nothing, which is true of ONE
# rung beside bounded ones — but `Floors::gating()` in concurrency_verdict.rs
# is `peak > 0 || any per_c > 0`, so ALL floors at zero flips the whole run to
# the INFO verdict, and `GateRecord::verdict_passes` accepts only "PASS"
# (gate/check.rs:382). Zeroing the ladder would have made this gate
# unsatisfiable: every campaign record INFO, the gate red forever. CI caught
# it; it is written here so the next person does not rediscover it.
#
# BASIS: the published ladder measured Atlas at 23.59 tok/s at C=1 on THIS
# instrument (bench/ladder38, 2026-08-17/18, this checkpoint, this box class).
# The floor is that x 0.5, rounded down to a clean step -> 11.5. The 2x margin
# is not timidity, it is the honest price of the one thing that differs: that
# leg ran an August build with its own CLI (fifo, 8 ssm slots, f16-pool,
# --mtp-gate force), and this gate serves main's recipe. A bar 2x under
# catches a collapse and cannot be reached by any drift or by noise.
# (The 2026-09-21 basis above is superseded by the 2026-09-23 re-cut at the
# top of this block.)
min = 22.0
noise = 0.0

[benchmarks.metrics.c2_aggregate_tok_s]
# ★ RE-CUT 2026-09-23, 20.5 -> 38.0, on the 128/1024 essay instrument (derivation
# block above the C1 table): latest 41.47 x 0.975 = 40.43 -> 40.0 fails the guard outright: dgx1 read
# 39.02 on the 2026-09-22 proof (n=22, range 39.02-41.69; dgx3 40.57-41.47).
# Written on the regime-minimum basis, 39.02 x 0.975 = 38.04 -> 38.0. Under
# the vLLM+MTP bar of 37.11 by 0.9 -- a real C=2 loss still reads on the site.
#
# ★ NOT RATCHETED 2026-09-20, deliberately. The policy floor on the latest
# value (31.66 -> 30.5, effective 29.55) fails the guard outright: three of
# the fourteen 32-slot-pin records read 26.74 / 27.40 / 27.43
# (2026-09-19-18d809f1ab and 2026-09-15-3c8809557c, dgx2; 2026-09-20-f648b50f86,
# dgx3) against eleven in 31.1-31.9 — the rung is still bimodal, exactly as
# the C1 block's 2026-09-11 survey found it, and a floor in the upper mode
# fails one healthy run in four. The regime-minimum basis (26.74 x 0.975 =
# 26.0, effective 25.05) is not written either: 26.0 was already proposed
# once from a biased set, and the 25.5 run in the C1 block's survey would
# clear that bar by 0.45, which is not a margin. 25.0 stayed in
# the gap between modes.
#
# ★ PROVISIONAL, 2026-09-21. THE BAR ABOVE WAS CUT AT THE RETIRED 512/320
# NATURAL INSTRUMENT AND IS NOT A BOUND ON THIS ONE — it sat 1.15x (C=1) to
# 4.3x (C=128) under what this instrument produces, so carrying it would have
# reported a guard that cannot fail while risking a false FAIL at C=1.
#
# ★ AND IT COULD NOT SIMPLY BE SWITCHED OFF. `RUNGS` says a rung with no
# metrics block keeps the 0.0 default and gates nothing, which is true of ONE
# rung beside bounded ones — but `Floors::gating()` in concurrency_verdict.rs
# is `peak > 0 || any per_c > 0`, so ALL floors at zero flips the whole run to
# the INFO verdict, and `GateRecord::verdict_passes` accepts only "PASS"
# (gate/check.rs:382). Zeroing the ladder would have made this gate
# unsatisfiable: every campaign record INFO, the gate red forever. CI caught
# it; it is written here so the next person does not rediscover it.
#
# BASIS: the published ladder measured Atlas at 41.02 tok/s at C=2 on THIS
# instrument (bench/ladder38, 2026-08-17/18, this checkpoint, this box class).
# The floor is that x 0.5, rounded down to a clean step -> 20.5. The 2x margin
# is not timidity, it is the honest price of the one thing that differs: that
# leg ran an August build with its own CLI (fifo, 8 ssm slots, f16-pool,
# --mtp-gate force), and this gate serves main's recipe. A bar 2x under
# catches a collapse and cannot be reached by any drift or by noise.
# (The 2026-09-21 basis above is superseded by the 2026-09-23 re-cut at the
# top of this block.)
min = 38.0
noise = 0.0

[benchmarks.metrics.c4_aggregate_tok_s]
# ★ RE-CUT 2026-09-23, 37.0 -> 67.0, on the 128/1024 essay instrument (derivation
# block above the C1 table): latest 73.58 x 0.975 = 71.74 -> 71.5 fails the guard outright: dgx1 read
# 69.05 on the 2026-09-22 resample (n=28, range 69.05-75.77; dgx1 alone
# 69.05-72.22). Regime-minimum basis, 69.05 x 0.975 = 67.32 -> 67.0. Sits
# 4.6 under the vLLM+MTP bar (71.61): Atlas wins C=4 by 1.03x and a floor at
# the bar would fail a healthy dgx1 run.
#
# ★ RATCHETED 2026-09-20, 37.5 -> 43.5 (effective 35.7 -> 41.7 after the
# unchanged `noise` of 1.8), on the REGIME-MINIMUM basis, not the policy one. The
# policy floor on 2026-09-20-43a3c9b202 (53.17 -> 51.5, effective 49.7) fails
# the guard: the 32-slot-pin records alone read 47.04-53.84 (n=14, sigma 2.27,
# two modes near 47.5 and 53), and dgx1 — no record on this pin yet — read
# 44.66-51.5 on the 8-slot pin (2026-09-12-8b33b70627 is the 44.66). The floor
# of 43.5 is 44.66 x 0.975 rounded down; it clears every one of the 22
# committed runs since 2026-09-08, on every box, by at least 2.9. Re-cut
# toward the policy once dgx1 has recorded on the 32-slot pin and the two
# modes are understood (avarok#812 is still the open question at this rung).
#
# ★ PROVISIONAL, 2026-09-21. THE BAR ABOVE WAS CUT AT THE RETIRED 512/320
# NATURAL INSTRUMENT AND IS NOT A BOUND ON THIS ONE — it sat 1.15x (C=1) to
# 4.3x (C=128) under what this instrument produces, so carrying it would have
# reported a guard that cannot fail while risking a false FAIL at C=1.
#
# ★ AND IT COULD NOT SIMPLY BE SWITCHED OFF. `RUNGS` says a rung with no
# metrics block keeps the 0.0 default and gates nothing, which is true of ONE
# rung beside bounded ones — but `Floors::gating()` in concurrency_verdict.rs
# is `peak > 0 || any per_c > 0`, so ALL floors at zero flips the whole run to
# the INFO verdict, and `GateRecord::verdict_passes` accepts only "PASS"
# (gate/check.rs:382). Zeroing the ladder would have made this gate
# unsatisfiable: every campaign record INFO, the gate red forever. CI caught
# it; it is written here so the next person does not rediscover it.
#
# BASIS: the published ladder measured Atlas at 74.21 tok/s at C=4 on THIS
# instrument (bench/ladder38, 2026-08-17/18, this checkpoint, this box class).
# The floor is that x 0.5, rounded down to a clean step -> 37.0. The 2x margin
# is not timidity, it is the honest price of the one thing that differs: that
# leg ran an August build with its own CLI (fifo, 8 ssm slots, f16-pool,
# --mtp-gate force), and this gate serves main's recipe. A bar 2x under
# catches a collapse and cannot be reached by any drift or by noise.
# (The 2026-09-21 basis above is superseded by the 2026-09-23 re-cut at the
# top of this block.)
min = 67.0
noise = 0.0

[benchmarks.metrics.c8_aggregate_tok_s]
# ★ RE-CUT 2026-09-23, 62.5 -> 110, on the 128/1024 essay instrument (derivation
# block above the C1 table): latest 125.46 x 0.975 = 122.32 -> 120 fails the guard outright: dgx1 read
# 119.84 on the 2026-09-22 proof (n=31, range 119.84-127.85). Regime-
# minimum basis, 119.84 x 0.975 = 116.84 -> 110 (the 10-step above 100, which
# is why this lands 8.2% under the minimum rather than 2.5%).
#
# ★ RATCHETED 2026-09-20, 48.0 -> 57.0 (effective 45.7 -> 54.7 after the
# unchanged `noise` of 2.3), on the REGIME-MINIMUM basis. The policy floor on
# 2026-09-20-43a3c9b202 (64.73 -> 63.0, effective 60.7) clears the fourteen
# 32-slot-pin records (62.40-70.08) but fails the guard on dgx1: its four
# 8-slot-pin runs read 58.83 / 61.4 / 60.6 / 59.4 (2026-09-11-da446c920b is
# the 58.83) against 62.9-63.0 on dgx2 on the same days — a ~3 tok/s box gap
# at this rung, an order of magnitude above the policy's margin. dgx2/dgx3
# rose ~3 when the pin moved, so dgx1 may have too; "may" is not a record.
# 57.0 was 58.83 x 0.975 rounded down; it cleared every committed
# run since 2026-09-08 by at least 4.1. Re-cut toward the policy once dgx1
# has recorded on the 32-slot pin.
#
# ★ PROVISIONAL, 2026-09-21. THE BAR ABOVE WAS CUT AT THE RETIRED 512/320
# NATURAL INSTRUMENT AND IS NOT A BOUND ON THIS ONE — it sat 1.15x (C=1) to
# 4.3x (C=128) under what this instrument produces, so carrying it would have
# reported a guard that cannot fail while risking a false FAIL at C=1.
#
# ★ AND IT COULD NOT SIMPLY BE SWITCHED OFF. `RUNGS` says a rung with no
# metrics block keeps the 0.0 default and gates nothing, which is true of ONE
# rung beside bounded ones — but `Floors::gating()` in concurrency_verdict.rs
# is `peak > 0 || any per_c > 0`, so ALL floors at zero flips the whole run to
# the INFO verdict, and `GateRecord::verdict_passes` accepts only "PASS"
# (gate/check.rs:382). Zeroing the ladder would have made this gate
# unsatisfiable: every campaign record INFO, the gate red forever. CI caught
# it; it is written here so the next person does not rediscover it.
#
# BASIS: the published ladder measured Atlas at 125.95 tok/s at C=8 on THIS
# instrument (bench/ladder38, 2026-08-17/18, this checkpoint, this box class).
# The floor is that x 0.5, rounded down to a clean step -> 62.5. The 2x margin
# is not timidity, it is the honest price of the one thing that differs: that
# leg ran an August build with its own CLI (fifo, 8 ssm slots, f16-pool,
# --mtp-gate force), and this gate serves main's recipe. A bar 2x under
# catches a collapse and cannot be reached by any drift or by noise.
# (The 2026-09-21 basis above is superseded by the 2026-09-23 re-cut at the
# top of this block.)
min = 110
noise = 0.0

[benchmarks.metrics.c16_aggregate_tok_s]
# ★ RE-CUT 2026-09-23, 100 -> 180, on the 128/1024 essay instrument (derivation
# block above the C1 table): latest 199.82 x 0.975 = 194.82 -> 190 clears the regime min of 192.22
# (dgx1 idle stack tip, 2026-08-20; n=28, dgx1 192.22-198.03) by 1.2% only,
# under the policy's own margin, and the CONTENDED dgx1 run of the same hour
# read 181.41 -- so the regime-minimum basis is written: 192.22 x 0.975 =
# 187.41 -> 180.
#
# ★ RATCHETED 2026-09-20, 84.0 -> 86.0 (effective 82.55 -> 84.55 after the
# unchanged `noise` of 1.45), on the REGIME-MINIMUM basis. The policy floor on
# 2026-09-20-43a3c9b202 (91.76 -> 89.5, effective 88.05) fails the guard by
# 0.56: the regime minimum is 88.61 (2026-09-18-0719dea47d, dgx3; n=22, sigma
# 1.39, range 88.61-93.55), and the widened-instrument history before the
# regime dips to 87.8 (2026-08-30-f0f6e48845). This is the rung the C1 block
# already named as the proof that a fresh set is no substitute for reading
# the history. 86.0 was 88.61 x 0.975 rounded down; it cleared
# every committed run on the widened instrument (since 2026-08-30) by at
# least 3.2.
#
# ★ PROVISIONAL, 2026-09-21. THE BAR ABOVE WAS CUT AT THE RETIRED 512/320
# NATURAL INSTRUMENT AND IS NOT A BOUND ON THIS ONE — it sat 1.15x (C=1) to
# 4.3x (C=128) under what this instrument produces, so carrying it would have
# reported a guard that cannot fail while risking a false FAIL at C=1.
#
# ★ AND IT COULD NOT SIMPLY BE SWITCHED OFF. `RUNGS` says a rung with no
# metrics block keeps the 0.0 default and gates nothing, which is true of ONE
# rung beside bounded ones — but `Floors::gating()` in concurrency_verdict.rs
# is `peak > 0 || any per_c > 0`, so ALL floors at zero flips the whole run to
# the INFO verdict, and `GateRecord::verdict_passes` accepts only "PASS"
# (gate/check.rs:382). Zeroing the ladder would have made this gate
# unsatisfiable: every campaign record INFO, the gate red forever. CI caught
# it; it is written here so the next person does not rediscover it.
#
# BASIS: the published ladder measured Atlas at 203.36 tok/s at C=16 on THIS
# instrument (bench/ladder38, 2026-08-17/18, this checkpoint, this box class).
# The floor is that x 0.5, rounded down to a clean step -> 100. The 2x margin
# is not timidity, it is the honest price of the one thing that differs: that
# leg ran an August build with its own CLI (fifo, 8 ssm slots, f16-pool,
# --mtp-gate force), and this gate serves main's recipe. A bar 2x under
# catches a collapse and cannot be reached by any drift or by noise.
# (The 2026-09-21 basis above is superseded by the 2026-09-23 re-cut at the
# top of this block.)
min = 180
noise = 0.0

[benchmarks.metrics.c32_aggregate_tok_s]
# ★ RE-CUT 2026-09-23, 140 -> 260, on the 128/1024 essay instrument (derivation
# block above the C1 table): latest 285.74 x 0.975 = 278.60 -> 270 clears the regime min of 274.68
# (dgx1 idle stack tip, 2026-08-20; n=28, dgx1 274.68-281.95) by 1.7% only;
# the contended dgx1 run read 261.63. Regime-minimum basis: 274.68 x 0.975 =
# 267.81 -> 260.
#
# ★ RATCHETED 2026-09-20, 98.5 -> 100 (effective 96.8 -> 98.3 after the
# unchanged `noise` of 1.7): 100 was the policy floor on the
# 2026-09-20-43a3c9b202 value 104.99 (x 0.975 = 102.37, rounded DOWN to the
# 10-step that applies from 100 up — which is why it lands 4.8% under the
# latest value and not 2.5%). Guard: regime min 103.60 (2026-09-13-1a0dc88a8c,
# dgx1; n=22, sigma 0.49), 5.3 above the effective bar.
#
# ★ PROVISIONAL, 2026-09-21. THE BAR ABOVE WAS CUT AT THE RETIRED 512/320
# NATURAL INSTRUMENT AND IS NOT A BOUND ON THIS ONE — it sat 1.15x (C=1) to
# 4.3x (C=128) under what this instrument produces, so carrying it would have
# reported a guard that cannot fail while risking a false FAIL at C=1.
#
# ★ AND IT COULD NOT SIMPLY BE SWITCHED OFF. `RUNGS` says a rung with no
# metrics block keeps the 0.0 default and gates nothing, which is true of ONE
# rung beside bounded ones — but `Floors::gating()` in concurrency_verdict.rs
# is `peak > 0 || any per_c > 0`, so ALL floors at zero flips the whole run to
# the INFO verdict, and `GateRecord::verdict_passes` accepts only "PASS"
# (gate/check.rs:382). Zeroing the ladder would have made this gate
# unsatisfiable: every campaign record INFO, the gate red forever. CI caught
# it; it is written here so the next person does not rediscover it.
#
# BASIS: the published ladder measured Atlas at 291.01 tok/s at C=32 on THIS
# instrument (bench/ladder38, 2026-08-17/18, this checkpoint, this box class).
# The floor is that x 0.5, rounded down to a clean step -> 140. The 2x margin
# is not timidity, it is the honest price of the one thing that differs: that
# leg ran an August build with its own CLI (fifo, 8 ssm slots, f16-pool,
# --mtp-gate force), and this gate serves main's recipe. A bar 2x under
# catches a collapse and cannot be reached by any drift or by noise.
# (The 2026-09-21 basis above is superseded by the 2026-09-23 re-cut at the
# top of this block.)
min = 260
noise = 0.0

[benchmarks.metrics.c64_aggregate_tok_s]
# ★ RE-CUT 2026-09-23, 190 -> 360, on the 128/1024 essay instrument (derivation
# block above the C1 table): latest 385.89 x 0.975 = 376.24 -> 370 clears the regime min of 375.08
# (dgx1 C=64/C=128 cross-box run, 2026-08-20; n=13, dgx1 375.08-379.91) by
# 1.4% only. Regime-minimum basis: 375.08 x 0.975 = 365.70 -> 360. Note the
# vLLM+MTP bar here is 361.39: Atlas wins C=64 by 1.07x and the floor sits
# just under the bar, not above it.
#
# ★ RATCHETED 2026-09-20, 109.5 -> 110 (effective 107.6 -> 108.1 after the
# unchanged `noise` of 1.9): 110 was the policy floor on the
# 2026-09-20-43a3c9b202 value 116.38 (x 0.975 = 113.47, rounded down to the
# 10-step). Guard: regime min 114.57 (2026-09-11-da446c920b, dgx1; n=22, sigma
# 0.62), 6.5 above the effective bar.
#
# ★ PROVISIONAL, 2026-09-21. THE BAR ABOVE WAS CUT AT THE RETIRED 512/320
# NATURAL INSTRUMENT AND IS NOT A BOUND ON THIS ONE — it sat 1.15x (C=1) to
# 4.3x (C=128) under what this instrument produces, so carrying it would have
# reported a guard that cannot fail while risking a false FAIL at C=1.
#
# ★ AND IT COULD NOT SIMPLY BE SWITCHED OFF. `RUNGS` says a rung with no
# metrics block keeps the 0.0 default and gates nothing, which is true of ONE
# rung beside bounded ones — but `Floors::gating()` in concurrency_verdict.rs
# is `peak > 0 || any per_c > 0`, so ALL floors at zero flips the whole run to
# the INFO verdict, and `GateRecord::verdict_passes` accepts only "PASS"
# (gate/check.rs:382). Zeroing the ladder would have made this gate
# unsatisfiable: every campaign record INFO, the gate red forever. CI caught
# it; it is written here so the next person does not rediscover it.
#
# BASIS: the published ladder measured Atlas at 386.63 tok/s at C=64 on THIS
# instrument (bench/ladder38, 2026-08-17/18, this checkpoint, this box class).
# The floor is that x 0.5, rounded down to a clean step -> 190. The 2x margin
# is not timidity, it is the honest price of the one thing that differs: that
# leg ran an August build with its own CLI (fifo, 8 ssm slots, f16-pool,
# --mtp-gate force), and this gate serves main's recipe. A bar 2x under
# catches a collapse and cannot be reached by any drift or by noise.
# (The 2026-09-21 basis above is superseded by the 2026-09-23 re-cut at the
# top of this block.)
min = 360
noise = 0.0

[benchmarks.metrics.c128_aggregate_tok_s]
# ★ RE-CUT 2026-09-23, 230 -> 440, on the 128/1024 essay instrument (derivation
# block above the C1 table): latest 478.91 x 0.975 = 466.94 -> 460 fails the guard outright: dgx1 read
# 459.32 on the 2026-08-20 cross-box run (n=13, range 459.32-482.05; the
# 2026-09-22 dgx1 proof 468.44-469.14). Regime-minimum basis: 459.32 x 0.975
# = 447.84 -> 440. Well above the vLLM+MTP bar of 358.57 (Atlas 1.34x).
#
# ★ RATCHETED 2026-09-20, 109.5 -> 110 (effective 107.6 -> 108.1 after the
# unchanged `noise` of 1.9): 110 was the policy floor on the
# 2026-09-20-43a3c9b202 value 116.26 (x 0.975 = 113.35, rounded down to the
# 10-step). Guard: regime min 114.00 (2026-09-11-da446c920b, dgx1; n=22, sigma
# 0.72), 5.9 above the effective bar.
#
# ★ PROVISIONAL, 2026-09-21. THE BAR ABOVE WAS CUT AT THE RETIRED 512/320
# NATURAL INSTRUMENT AND IS NOT A BOUND ON THIS ONE — it sat 1.15x (C=1) to
# 4.3x (C=128) under what this instrument produces, so carrying it would have
# reported a guard that cannot fail while risking a false FAIL at C=1.
#
# ★ AND IT COULD NOT SIMPLY BE SWITCHED OFF. `RUNGS` says a rung with no
# metrics block keeps the 0.0 default and gates nothing, which is true of ONE
# rung beside bounded ones — but `Floors::gating()` in concurrency_verdict.rs
# is `peak > 0 || any per_c > 0`, so ALL floors at zero flips the whole run to
# the INFO verdict, and `GateRecord::verdict_passes` accepts only "PASS"
# (gate/check.rs:382). Zeroing the ladder would have made this gate
# unsatisfiable: every campaign record INFO, the gate red forever. CI caught
# it; it is written here so the next person does not rediscover it.
#
# BASIS: the published ladder measured Atlas at 478.11 tok/s at C=128 on THIS
# instrument (bench/ladder38, 2026-08-17/18, this checkpoint, this box class).
# The floor is that x 0.5, rounded down to a clean step -> 230. The 2x margin
# is not timidity, it is the honest price of the one thing that differs: that
# leg ran an August build with its own CLI (fifo, 8 ssm slots, f16-pool,
# --mtp-gate force), and this gate serves main's recipe. A bar 2x under
# catches a collapse and cannot be reached by any drift or by noise.
# (The 2026-09-21 basis above is superseded by the 2026-09-23 re-cut at the
# top of this block.)
min = 440
noise = 0.0

[benchmarks.metrics.peak_aggregate_tok_s]
# ★ RE-CUT 2026-09-23, 230 -> 440, on the 128/1024 essay instrument (derivation
# block above the C1 table): the peak is the C=128 cell in every regime run that measured both top rungs
# (dgx1 379.8 -> 468.4, dgx2 389.6 -> 482.1, gate 385.9 -> 478.9), so the
# floor is the C=128 floor, 440, cut in the block above. A sweep whose peak
# fell under this has lost its top rung whichever C produced it.
#
# ★ RATCHETED 2026-09-20, 109.5 -> 110 (effective 107.6 -> 108.1 after the
# unchanged `noise` of 1.90): 110 was the policy floor on the
# 2026-09-20-43a3c9b202 peak 116.38 (at C=64; x 0.975 = 113.47, rounded down
# to the 10-step). Guard: regime min 114.57 (2026-09-11-da446c920b, dgx1;
# n=22), 6.5 above the effective bar. The peak still lands at C=64 or C=128
# in every regime record.
#
# The peak rung is NO LONGER C=16. On the widened ladder it lands at C=64 in
# two of three reps and C=128 in the third (115.52 / 115.32 / 115.44), the
# curve having saturated near 115 from C=64 on. The floor is therefore the
# C=64/C=128 bar, not the C=16 one the previous comment described: a sweep
# whose peak fell below this has lost its top rung whichever C produced it.
#
# ★ PROVISIONAL, 2026-09-21. THE BAR ABOVE WAS CUT AT THE RETIRED 512/320
# NATURAL INSTRUMENT AND IS NOT A BOUND ON THIS ONE — it sat 1.15x (C=1) to
# 4.3x (C=128) under what this instrument produces, so carrying it would have
# reported a guard that cannot fail while risking a false FAIL at C=1.
#
# ★ AND IT COULD NOT SIMPLY BE SWITCHED OFF. `RUNGS` says a rung with no
# metrics block keeps the 0.0 default and gates nothing, which is true of ONE
# rung beside bounded ones — but `Floors::gating()` in concurrency_verdict.rs
# is `peak > 0 || any per_c > 0`, so ALL floors at zero flips the whole run to
# the INFO verdict, and `GateRecord::verdict_passes` accepts only "PASS"
# (gate/check.rs:382). Zeroing the ladder would have made this gate
# unsatisfiable: every campaign record INFO, the gate red forever. CI caught
# it; it is written here so the next person does not rediscover it.
#
# BASIS: the published ladder measured Atlas at 478.11 tok/s at C=64/C=128 on THIS
# instrument (bench/ladder38, 2026-08-17/18, this checkpoint, this box class).
# The floor is that x 0.5, rounded down to a clean step -> 230. The 2x margin
# is not timidity, it is the honest price of the one thing that differs: that
# leg ran an August build with its own CLI (fifo, 8 ssm slots, f16-pool,
# --mtp-gate force), and this gate serves main's recipe. A bar 2x under
# catches a collapse and cannot be reached by any drift or by noise.
# (The 2026-09-21 basis above is superseded by the 2026-09-23 re-cut at the
# top of this block.)
min = 440
noise = 0.0

[benchmarks.metrics.min_completion_tokens]
# ★ RE-CUT 2026-09-23, 260 -> 650, with the nine rung floors and from the same
# records, as the paragraph below asked. The bar is the per-run minimum
# completion over ALL requests, so the sample is each regime run's minimum
# completion_tokens_per_req (n=27 runs plus the gate record): 676 (dgx1
# C=64/C=128 cross-box run, 2026-08-20), 691, 691 (rounds 10/11 at C=128),
# 717 (dgx1 proof 2026-09-22), 745, 745, 745, 753, ... and 892 on the gate
# record. Guard arm as for the rungs: 676 x 0.975 = 659.1 -> 650 on the
# 10-step. 0.8 x 1024 = 819 (the vacuity majority rule) is NOT written: three
# clean runs stopped one request under it, deterministically at the widest
# rungs, and a floor above a measured healthy minimum is a bootstrap.
#
# ★ KEPT AT 260 THROUGH THE 2026-09-21 RE-POINT, deliberately, while every
# rung floor went to 0.0 — this is the one bar the re-point does not
# invalidate, and it is not redundant with vacuous_cells.
#
# The sentence not to get wrong: the vacuity rule is NOT "no request under
# 80% of the budget". concurrency_vacuity.rs was rewritten FOR this very
# instrument — its module doc cites l38_r{7..11}_c*.json, where rep 2 of
# C=128 deterministically stops one request at 691 tokens and C=64 one at
# 753 — and now asks only for cell OCCUPANCY >= 0.8 of the total budget plus
# a MAJORITY of requests over 0.8 x osl. A minority may deliver anything down
# to zero. min_completion_tokens is what still denies that minority the
# floor, so switching it off with the rung floors would have removed a guard
# vacuity never replaced.
#
# 260 is loose here and is left loose ON PURPOSE rather than re-cut by
# arithmetic. It was calibrated against the natural fixture's ~296-token
# deterministic completion; the essay fixture's own minimum has not been
# measured on this gate. The published raw files' observed minima on this
# instrument are 691 (C=128) and 753 (C=64), so 260 passes everything ever
# seen while still failing a degenerate early-EOS collapse. Re-cut it with
# the nine rung floors from the same first records, not before: 0.8 x 1024 =
# 819 is the number arithmetic would suggest, and declaring it now would be
# a bootstrap.
min = 650.0

[benchmarks.metrics.vacuous_cells]
# Zero, absolutely: the driver already refuses to mint throughput keys from
# vacuous cells, and this pins the same fact at the record level.
max = 0.0

# --- concurrency-sweep-dflash2 ---------------------------------------------
# The same ladder with the DFlash2 drafter armed, REQUIRED since 2026-08-29.
#
# ★ WHY A SECOND GATE AND NOT A SECOND VARIANT OF THE FIRST. `check_record`'s
# `record_is_required_subject` refuses a non-default checkpoint for the
# required verdict — "a pass on the dense 27B is not evidence for the required
# gate, whose declared subject is the default checkpoint" — so a variant can
# never satisfy a mandatory gate. Two gate ids is the mechanism the repo
# already uses for exactly this shape (`bfcl-subset` / `bfcl-subset-echolp`).
#
# ★ WHY IT EXISTS AT ALL. Nothing else gates speculation. All ten previous
# required gates serve a no-drafter recipe, so a DFlash2 regression could only
# ever be judged by hand — which is precisely what the 2026-08 C4
# investigation had to do, spending days establishing "not DFlash2" from
# source reading because no committed record could speak for it.
#
# ★ ITS NUMBERS ARE NOT COMPARABLE TO concurrency-sweep's. Speculation moves
# low-C aggregate throughput by more than any regression either bar is set to
# catch, so the two carry independent bounds and must never be crossed. The
# useful comparison is each gate against its own history, never one against
# the other.
[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.8-27B-NVFP4"
gate = "concurrency-sweep-dflash2"
recipe = "qwen3.8/qwen3.8-27b-nvfp4-dflash2"
label = "Qwen3.8-27B dense (NVFP4, unsloth) + DFlash2"
default = true
status = "measured"
note = """\
MEASURED 2026-08-30, spark-256a, this branch's binary: 3 reps of the full \
ladder — C=1/2/4/8/16, isl 512, osl 200, natural (MinHeap) fixture, temp 0, \
seed 0, warmup 1 — zero vacuous cells and zero request errors in every rep. \
Aggregate tok/s per C: C1 23.42/26.60/26.63; C2 37.67/38.62/37.56; C4 \
44.58/44.17/44.77; C8 53.35/53.39/52.83; C16 60.90/60.76/60.89. Floors at \
mean - max(3*sigma, ~5%) per rung, cut together, never from a best rep. \
★ WHY osl=200 AND NOT THE PLAIN GATE'S 320. At 320 this gate is uncertifiable \
at ANY floor: the C=1 cell is deterministically vacuity-flagged. Measured \
three times before and three times after four throughput fixes, identically — \
vacuous cells 0/1/2 and min_completion 262/229/229 — because DFlash2's \
completion on this fixture is ~229 tokens, i.e. 71.5% of a 320 budget against \
an 80% vacuity floor. A vacuous cell fails unconditionally, so no threshold \
choice helps. This is the SAME calibration the plain entry documents making \
for itself in the other direction ("at osl=512 the C=16 cell was ALWAYS \
vacuity-flagged ... the default budget cannot measure the top rung"): lower \
the budget until every request hits it. RAISING osl so 229 clears 80% would \
be tuning to hide the rule — requests would keep ending naturally and only \
the bar would move. Lowering it below the natural stop makes every finish a \
"length" finish, which is the comparability property the rule protects. \
★ WHY gamma 8 AND NOT 15. Single-stream, gamma 15 is faster (57.0 median vs \
49.9) — but the verify pool is gamma-sized and gamma 15 at bs=16 refuses to \
START: 57.7 GB consumed + 45.7 GB reserve = 103.4 GB, exactly the util-0.85 \
budget, measured on a verified-idle box. gamma 8 also holds the best \
acceptance (73.5% vs 48.6%), which is what matters when the verify pool is \
shared across sequences. gamma 15's 57.0 is a SINGLE-STREAM result and must \
not be quoted as a concurrency one. \
★ NOT COMPARABLE TO concurrency-sweep, in either direction: different batch \
cap (16 vs 128) and different osl (200 vs 320). Each gate is scored against \
its own committed history; crossing them is meaningless."""

[benchmarks.serve_overrides]
# The DFlash2 serve. It matches the plain gate's profile on every axis it CAN
# match; the batch cap is the one it cannot, and the reason is measured rather
# than assumed.
#
# ★ 16, NOT 128 — a DFlash2 serve refuses to START wider on this box.
# Measured 2026-08-29, spark-256a, util 0.85, everything else as pinned here:
#
#     max_batch_size |  48  |  64  |  96  | 128
#     reserve        | 56.5 | 59.0 | 63.8 | 68.7  GB
#
# against ~46.5 GB left after weights + drafter, so 128 dies with "No memory
# left for KV cache: 56.9 GB consumed + 67.1 GB inference reserve". The slope
# is only ~151 MB/slot; the ~49 GB INTERCEPT is the wall.
#
# It is the verify pool. `preflight` at bs=64 prints
# `ssm_pool=53568 MB (64 max_batch blobs + 32 MTP-covered slots x 3 verify
# blobs)`; the same box with MTP instead prints 32112 MB for 128 max_batch
# blobs + 32 slots x 5. DFlash blobs are ~2.5x MTP's because the pools are
# gamma-sized -- uniform K = gamma+1 on every slot -- and this drafter's
# trained block_size is 8.
#
# Three things that look like levers and are not:
#   * AVAROK_MTP_MAX_SEQS cannot shrink the slot count. `mtp_state_slots_with`
#     is `max_batch_size.min(cap.max(VERIFY_WY_TABLE_SEQS))` and that floor is
#     32. Setting it to 8 changed nothing (verified). The floor only lifts
#     BELOW bs=32, where max_batch_size becomes the binding term -- which is
#     exactly why 16 is comfortable and 48 is not: it is a knee, not a slope.
#   * --ssm-h-dtype f16-pool, the lever that took the MTP bs=128 reserve
#     36.7 -> 22.4 GB, is REJECTED with --dflash by cli/validate.rs: the
#     DFlash verify width dispatches gated_delta_rule_wy17, which has no FP16
#     h-state twin and strides its h intermediates in FP32 elements. "Fluent
#     garbage, not an error." The refusal is right; the relief is unavailable.
#   * Raising --gpu-memory-utilization past 0.85 is not on the table on GB10.
#
# So this gate's ladder stops at C=16 (its param_overrides below), because a
# rung above the batch cap measures the cap and not the engine -- the same
# defect as the batch-1 instrument the plain gate's pin exists to correct, one
# scale up. The real fix is an FP16 h-state twin for gated_delta_rule_wy17;
# until then this is the honest width.
max_batch_size = "16"
kv_cache_dtype = "fp8"
# 32, with the plain gate (2026-09-13): the one-variable rule — every pin the
# plain gate carries is carried here identically except the batch cap — and
# the same warm-capacity arithmetic (`concurrency::slots_needed`; see the
# plain gate's note). 4.73 GiB beside the drafter fits the bs=16 reserve.
ssm_cache_slots = "32"
max_model_len = "4096"
# --dflash is `conflicts_with = "speculative"` at the clap level, so the
# recipe this names must not also enable MTP; the two drafters are mutually
# exclusive by construction, not by convention.
dflash = "true"
# Named explicitly rather than left to the MODEL.toml [dflash] fallback. A
# drafter is trained against ONE target's hidden states — this one consumes
# Qwen3.8-27B layers [5,19,33,47,61] — and a mismatched drafter degrades to
# near-zero acceptance rather than failing, which is slow and silent. The
# record therefore has to carry which drafter produced the number.
draft_model = "incoai/Qwen3.8-27B-DFlash2"
# gamma=8 is the drafter's OWN trained block_size, stated inside its
# `dflash_config`. It must be explicit: --dflash-gamma's clap default is 16,
# which shadows the checkpoint's value, and running an 8-block drafter at 16
# measured 0% accept on every verify step while sizing its pools for twice
# the block it can use.
dflash_gamma = "8"

[benchmarks.param_overrides]
# ★ NO LONGER THE PLAIN GATE'S INSTRUMENT. Until 2026-09-21 this said "isl
# and osl are identical, so the rungs the two ladders SHARE are directly
# comparable"; that stopped being true when the plain gate was re-pointed to
# the published ladder (isl 128 / osl 1024 / essay). This entry deliberately
# stays at isl 512 / osl 200 / natural: its bars were cut there, DFlash2 is
# not on the published ladder and never was, and re-pointing it would cost a
# second set of floors to buy a comparison nothing draws. The two ladders are
# now comparable to NEITHER each other nor the published pair — which the
# entry note below already required of them for a different reason (speculation
# moves low-C throughput by more than either bar is set to catch).
# Rungs are still truncated at the widest this serve can admit (see the
# batch-cap note above).
concurrencies = "1,2,4,8,16"
isls = "512"
# 200, not the plain gate's 320 — see the osl note on this entry. Every request
# hits the budget at 200; at 320 the C=1 cell is deterministically vacuous.
osl = "200"

# ── RE-CUT 2026-09-20 under the SPEED-BOUND POLICY (owner, 2026-09-20; stated
# in full above the plain ladder's metric tables): floor = latest measured x
# 0.975 rounded DOWN to a clean step (0.5 below 100). Source record:
# .benchmarks/concurrency-sweep-dflash2/2026-09-20-43a3c9b202.json (spark-28c2
# = dgx3, 2026-09-20 12:04 UTC, on the current 32-slot pin), which reads
#
#     C1 25.06  C2 45.43  C4 57.80  C8 66.52  C16 67.25  peak 67.25 (at C=16)
#
# Same GUARD as the plain ladder: the effective bar (min - noise) must not
# exceed any committed PASS value of the rung since the 2026-09-08 step (n=22,
# five boxes; the 8-slot records of 2026-09-08..13 included because dgx1 has
# no record on the 32-slot pin). On THIS ladder the pin change moved the mid
# rungs by far more than any margin — C4 ~44 -> ~56 and C8 ~56 -> ~69 on
# dgx2/dgx3 between 2026-09-13 and 2026-09-14 — so dgx1's 8-slot values
# dominate the guard at C4/C8 and hold those floors well under the current
# level. That is the honest cost of one unmeasured box; re-cut once it records.
#
#     rung  latest  policy  regime min (record, box)              written  basis
#     C1    25.06   24.0    24.69 (09-12-8b33b70627, dgx1)        24.0     policy
#     C2    45.43   44.0    41.96 (09-08-e63bedf098, edgexpert)   40.5     regime min
#     C4    57.80   56.0    42.24 (09-13-1a0dc88a8c, dgx1)        42.5     KEPT (41.0 < 42.5)
#     C8    66.52   64.5    55.36 (09-11-da446c920b, dgx1)        54.0     regime min
#     C16   67.25   65.5    60.98 (09-08-e63bedf098, edgexpert)   59.0     regime min
#     peak  67.25   65.5    60.98 (09-08-e63bedf098, edgexpert)   59.0     regime min

[benchmarks.metrics.c1_aggregate_tok_s]
# ★ RATCHETED 2026-09-20, 23.7 -> 24.0 (effective 22.75 -> 23.05 after the
# unchanged noise 0.95): the floor of 24.0 is the policy floor on the
# 2026-09-20-43a3c9b202 value 25.06 (x 0.975 = 24.43, rounded down). Guard:
# regime min 24.69 (2026-09-12-8b33b70627, dgx1; n=22, sigma 0.14), 1.6 above
# the effective bar.
#
# ★ RATCHETED 2026-09-11, 20.0 -> 23.7 (effective 22.75 after noise 0.95),
# alongside the plain ladder's C=1 rung and for the same reason: this rung's
# LEVEL moved and the floor did not follow, so 30% of the measured throughput
# was unguarded.
#
#     pre-step   n=7   21.72-23.46
#     post-step  n=4   24.77-24.86   (committed records)
#
# The set behind 23.7 is twelve fresh gate runs on the current instrument,
# SIX ON EACH BOX, because a one-box set is what this session already got
# wrong once at this rung's plain-ladder sibling:
#
#     dgx1 (spark-256a)  n=6  24.4 24.6 24.6 24.8 24.7 24.7   mean 24.633
#     dgx2 (spark-43fa)  n=6  25.0 24.8 24.9 24.9 24.8 24.9   mean 24.883
#     combined           n=12 mean 24.758  sigma 0.161
#
# mean - max(3*sigma, 5%) is 23.52, the 5% arm governing. 23.7/0.95 lands at
# 22.75, i.e. DELIBERATELY BELOW the formula: it keeps 6.8% under the WORST
# observed run rather than sitting on the mean-derived bar, so a regression
# has to be real to fail it.
#
# ★ What it now catches that 19.05 did not: a regression halving the step
# lands near 23.3 and FAILS. At the superseded 19.05 a regression could have
# undone the entire step -- back to the 21.7-23.5 pre-step band -- and still
# passed, silently.
min = 24.0
noise = 0.95

[benchmarks.metrics.c2_aggregate_tok_s]
# ★ RATCHETED 2026-09-20, 36.0 -> 40.5 (effective 35.38 -> 39.88 after the
# unchanged noise 0.62), on the REGIME-MINIMUM basis. The policy floor on
# 2026-09-20-43a3c9b202 (45.43 -> 44.0, effective 43.38) fails the guard:
# three regime records read 41.96-42.0 (2026-09-08-e63bedf098, edgexpert-aae5;
# 2026-09-08-3cfe4d8c45 and 2026-09-11-12c2875ca4, dgx2), and even the
# 32-slot-pin set is trimodal near 45.4 / 48.7 / 51.4 (n=14, sigma 2.12). The
# floor of 40.5 is 41.96 x 0.975 rounded down; it clears every committed run
# since 2026-09-08 by at least 2.1.
min = 40.5
noise = 0.62

[benchmarks.metrics.c4_aggregate_tok_s]
# ★ NOT RATCHETED 2026-09-20. The policy floor on 2026-09-20-43a3c9b202
# (57.80 -> 56.0, effective 55.27) fails the guard twice over: two 32-slot-pin
# records read 54.50 / 54.8 (2026-09-17-c9bd822f15, dgx2; 2026-09-17-8cd40290ad,
# dgx3), and dgx1 — no record on this pin — read 42.24-45.2 on the 8-slot pin
# (2026-09-13-1a0dc88a8c is the 42.24), because this rung's level moved WITH
# the pin (~44 -> ~56 on dgx2/dgx3 between 2026-09-13 and 2026-09-14). The
# regime-minimum basis, 42.24 x 0.975 = 41.0, is BELOW the floor of 42.5
# already in force, so nothing the data supports is a tightening. A floor cut
# from the 32-slot set alone (54.50 x 0.975 = 53.0) is the obvious next step
# and is deliberately not taken until dgx1 has recorded on this pin: at C=4
# dgx1 trailed dgx2/dgx3 by 1-2 tok/s on the old pin, and a floor that fails
# the slowest box's first run costs a certification campaign.
min = 42.5
noise = 0.73

[benchmarks.metrics.c8_aggregate_tok_s]
# ★ RATCHETED 2026-09-20, 50.5 -> 54.0 (effective 49.62 -> 53.12 after the
# unchanged noise 0.88), on the REGIME-MINIMUM basis. The policy floor on
# 2026-09-20-43a3c9b202 (66.52 -> 64.5, effective 63.62) clears all fourteen
# 32-slot-pin records (66.08-72.90, sigma 2.05) but fails the guard on dgx1,
# which has no record on this pin and read 55.36-56.0 on the 8-slot pin
# (2026-09-11-da446c920b is the 55.36); the rung's level moved with the pin
# (~56 -> ~69 on dgx2/dgx3), and dgx1 matched dgx2 to within 0.6 on the old
# pin, so it very probably moved too — "probably" is not a record. The floor
# of 54.0 is 55.36 x 0.975 rounded down; it clears every committed run since
# 2026-09-08 by at least 2.2. Re-cut toward the policy once dgx1 has recorded
# on the 32-slot pin.
min = 54.0
noise = 0.88

[benchmarks.metrics.c16_aggregate_tok_s]
# ★ RATCHETED 2026-09-20, 58.0 -> 59.0 (effective 57.0 -> 58.0 after the
# unchanged noise 1.0), on the REGIME-MINIMUM basis. The policy floor on
# 2026-09-20-43a3c9b202 (67.25 -> 65.5, effective 64.5) fails the guard: the
# rung reads 60.98-70.46 across the regime (n=22, sigma 2.92; 62.73 and 62.9
# on the 32-slot pin, 2026-09-17-456a4f247e and 2026-09-17-8cd40290ad),
# because C=16 is the batch cap on this serve and measures the cap, not the
# engine (see the serve pin note). The floor of 59.0 is 60.98 x 0.975 rounded
# down; it clears every committed run since 2026-09-08 by at least 3.0.
min = 59.0
noise = 1.0

[benchmarks.metrics.peak_aggregate_tok_s]
# The peak rung is NO LONGER C=16 by construction on this ladder. Since the
# 32-slot pin (2026-09-14) the peak lands at C=8 in ten of fourteen records
# and at C=16 in four, so the peak floor is bounded by the C=8/C=16 bars and
# no longer equals the C=16 one.
# ★ RATCHETED 2026-09-20, 58.0 -> 59.0 (effective 57.0 -> 58.0 after the
# unchanged noise 1.00), on the REGIME-MINIMUM basis: the policy floor on
# 2026-09-20-43a3c9b202 (67.25 -> 65.5, effective 64.5) fails the guard on the
# 8-slot records, whose peak WAS the C=16 cell (60.98, 2026-09-08-e63bedf098).
# The floor of 59.0 is 60.98 x 0.975 rounded down; every committed peak since
# 2026-09-08 clears it by at least 3.0, and every 32-slot-pin peak
# (66.08-72.90) by at least 8.
min = 59.0
noise = 1.00

[benchmarks.metrics.min_completion_tokens]
# The vacuity guarantee at THIS gate's budget. Observed 174/187/200 at osl 200
# (vacuity floor 160); 164 sits below the minimum with margin and above the
# 80%-of-osl rule, so it stays the stricter of the two.
min = 164.0

[benchmarks.metrics.vacuous_cells]
# Zero, absolutely. Two of three reps had a vacuous cell at osl 320 and this
# gate could not be certified at all; the osl re-calibration is what removed
# them, not a lowered bar.
max = 0.0

# --- vision-fidelity -------------------------------------------------------
# Vision models only. This gate is declared on the three targets that ship a
# vision tower and have a BENCH.toml; a text-only target has no entry and the
# gate simply does not apply to it.
#
# The bounds are ABSOLUTE and take no noise term, like the cross-contamination
# gate's. Vision-token count is arithmetic over the patch grid -- there is no
# run-to-run variance for a tolerance to absorb, so any movement is a real
# change in preprocessing. That is the property that makes this gate worth
# requiring: the 2026-08-14 resolution-cap defect silently reduced every image
# above 1280px to about a tenth of its permitted area, and every capability
# check passed throughout, because a downscaled picture is still recognisably
# what it is.
#
#   integrity_passed  = 10  the INTEGRITY floor, and the legs a single
#                           well-formed request cannot reach. Every one of
#                           them fails as a FLUENT ANSWER TO THE WRONG INPUT
#                           rather than an error, which is the class this
#                           repo keeps rediscovering:
#                             heterogeneous-concurrency  four DIFFERENT
#                               requests in flight (one text-only), each
#                               scored against its own input. Identical
#                               requests cannot test the per-request offsets
#                               into the shared ViT output buffer, because
#                               every offset is then interchangeable.
#                             prefix-cache-isolation     same prompt text,
#                               different image, back to back. Guards the
#                               predicate that keeps vision prompts out of
#                               the prefix cache — it silently missed the
#                               VIDEO pad token until 2026-08-14.
#                             long-prompt-splice         a prompt past the
#                               single-chunk budget, which leaves the batched
#                               co-dispatch encode for a second splice in a
#                               different file. Measured 81 vs 7082 prompt
#                               tokens, so it really does cross over.
#                             media-in-history           the image in an
#                               earlier turn, not in the message asking the
#                               question. The #165 path.
#                             exif-orientation-pinned    PINS current
#                               behaviour rather than judging it: EXIF
#                               orientation is IGNORED, measured 2026-08-14.
#                               A rotated phone photo reaches the model
#                               sideways. Whether to honour the tag is a
#                               product decision; that it must not change by
#                               accident is not.
#                             stream-blocking-parity     the SAME request
#                               streamed and not. Every other leg sets
#                               stream:true, so the blocking path — which
#                               assembles its own response — was never run.
#                               Both must answer correctly AND agree on
#                               prompt_tokens; a difference means the two
#                               paths built different prompts from identical
#                               bytes.
#                             multi-choice-image         n=2 with an image.
#                               The blocking path gives only choice 0 the
#                               pixels (the encode is shared), which is
#                               correct and is also index-conditional code
#                               nothing ran.
#                             image-on-tool-result       the image arriving
#                               on a role:"tool" message — the OTHER call
#                               site of collect_message_images, and the case
#                               #165 exists for.
#                             thinking-on-vision         the DEFAULT config
#                               for these checkpoints, which every other leg
#                               turns off. Thinking lengthens the prompt and
#                               so moves which prefill chunk the pad run lands
#                               in. Asserted as a DIFFERENCE between two
#                               images so the unknown <think> cost cancels.
#   integrity_measured = 10 a skipped integrity leg is not a passed one.
#   geometry_matched  = 14  the CORRECTNESS floor, and the one that moves when
#                           preprocessing changes. Rung 09 (1600x900) is the
#                           ONLY rung above the retired 1280px clamp: under the
#                           clamp it is 920 tokens, un-downscaled it is 1400.
#                           The other eight fit under either regime and so
#                           cannot tell the two apart -- without rung 09 this
#                           gate could not fail on the defect it exists for.
#   geometry_asserted = 14  the MEASUREMENT floor, which is a DIFFERENT failure.
#                           A cell the server cannot serve reports UNMEASURED,
#                           which is not a mismatch -- so without this floor an
#                           encoder-capacity regression would turn every cell
#                           UNMEASURED and still read as PASS. Both are kept
#                           because neither implies the other.
#   probes_passed     = 3   capability, all three.
#   control_held      = 1   the no-image control MUST hold. The non-vacuity
#                           requirement expressed as a threshold: if the control
#                           answers as though it saw a picture, the probes are
#                           not evidence and the run is VACUOUS.

[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.8-27B-NVFP4"
gate = "vision-fidelity"
recipe = "qwen3.8/qwen3.8-27b-nvfp4-unsloth"
label = "Qwen3.8-27B dense (NVFP4, unsloth)"
default = false
status = "measured"
note = """\
REFERENCE RUN 2026-08-14, dgx-00, binary feat/video-support @ 5a22d7ce: PASS, 14/14 geometry \
cells matched, 3/3 probes, 10/10 integrity legs, control held, 26.5 s. Geometry was IDENTICAL across all \
three vision targets (49/121/192/220/576/576/920/405/1400/1/128/49/49/49 merged \
tokens) -- token \
count is a property of the preprocessor rather than the checkpoint, so agreement \
across a dense NVFP4, an unsloth NVFP4 and an FP8 MoE is evidence the arithmetic is \
right rather than a coincidence. Ships preprocessor_config.json plus a separate \
video_preprocessor_config.json (unused until video support lands), so it resolved \
16.7M px before the resolver fix as well; clamped to the encoder's 16384-patch \
ceiling. Run at --max-seq-len 32768 for this record."""

[benchmarks.metrics.geometry_matched]
min = 14.0

[benchmarks.metrics.geometry_asserted]
min = 14.0

[benchmarks.metrics.integrity_passed]
min = 10.0

[benchmarks.metrics.integrity_measured]
min = 10.0

[benchmarks.metrics.probes_passed]
min = 3.0

[benchmarks.metrics.control_held]
min = 1.0

# --- video-fidelity --------------------------------------------------------
# VIDEO MODELS ONLY. This target owns the gate's default serve; as of
# 2026-08-15 all three vision checkpoints declare it, so nothing about the
# benchmark is 3.8-specific. The 35B was the last to be added, and only
# because it was the one target weak enough to expose the media-ordering
# defect -- see its BENCH.toml entry.
#
# ── 2026-08-15: this target went red, and the FIXTURE was at fault ─────────
# It scored 4/13, dropping "green" from the FORWARD clip (mp4 and gif alike)
# while reading the REVERSED clip perfectly 4/4 on identical geometry. Ruled
# out in order, each by measurement rather than argument:
#
#   * the media-ordering fix -- parent commit c915f01d, rebuilt and served
#     with the same flags, produced the SAME 4/13 leg for leg;
#   * the pipeline -- qwen3.6-27b read all four colours through the identical
#     code path, same 240 prompt tokens, and answered "SECOND segment ->
#     GREEN" where this target said "blue";
#   * the KV cache -- --kv-cache-dtype bf16 changed nothing, byte for byte;
#   * this checkpoint's double-quantisation (see the loader note below) --
#     Qwen/Qwen3.8-27B-FP8, the same weights in the block-scaled FP8 format
#     Atlas loads natively, failed the same way.
#
# The cause: the fixture's green was HTML green #008000, the one HALF-bright
# colour among three full-bright ones, because ffmpeg resolves the NAME
# "green" that way. Measured with the shade as the only variable:
#     #008000 -> "Red, Blue, Yellow"          (2/2 runs)
#     #00FF00 -> "Red, Green, Blue, Yellow"   (2/2 runs)
# Qwen3.6-27B read the dim band fine, so the difficulty stayed invisible until
# a weaker checkpoint became this gate's subject. The gate was measuring
# colour sensitivity, not frame order. Fixtures regenerated full-bright
# (scripts/gen_test_videos.py); all three targets now score 13/13 and the
# geometry is unchanged (67/116/214 tokens, 49 per group).
#
# ★ SEPARATE FINDING, NOT FIXED: this checkpoint is `format = mixed-precision`
# -- attention q/k/v/o, the GDN projections and lm_head are FP8 with a
# PER-CHANNEL scale, and only the MLP is NVFP4. Atlas's native w8a16 path
# needs a [N/128, K/128] block grid, so a per-row scale is deliberately
# refused (it would read another row's multiplier and produce garbage
# logits) and those tensors are dequantised to BF16 and RE-quantised to
# NVFP4. Measured cost, gate prompt, dark-green fixture: this checkpoint
# answered "Red, Blue" where the natively-loaded Qwen/Qwen3.8-27B-FP8
# managed "Red, Blue, Yellow". Both were wrong, so this is not what the red
# was -- but the double-quantisation is real and this target carries it.
# MODEL.toml's claim that the two 27B checkpoints' quantization_config
# "differs only in a tooling version string" is FALSE as of the checkpoint
# on disk; see the note there.
#
# ★ REQUIRES FFMPEG ON THE SERVER for every container except GIF, and the
# serve must carry `--video-allow-ffmpeg` — the subprocess decoder is gated
# behind that flag, so the recipe this entry names pins
# `video_allow_ffmpeg: true` (added 2026-08-15; without it the certified
# self-start 400'd every MP4 leg). When the flag or binary is absent those
# legs SKIP rather than fail at the benchmark level, but only a MANUAL run
# degrades to the GIF-only subset — this GATE's thresholds demand all 13 legs
# and 0 skips, so a certified run without ffmpeg is red by design. A run
# where everything skipped reports INCONCLUSIVE, never PASS.
#
# Bounds are absolute and take no noise term: every assertion is either
# arithmetic over the patch grid or an exact color sequence, so there is no
# run-to-run variance for a tolerance to absorb.
#
#   legs_passed   = 14  every leg. The ones that carry the weight:
#                         order fwd + REVERSED -- the same clip both ways,
#                           requiring the answer to reverse with it. The only
#                           assertion separating "the frames arrived in order"
#                           from "something arrived", and the one that caught
#                           the splice defect where video pad tokens received
#                           no encoder rows and the model described a gray
#                           field with every token count perfect.
#                         group-proportionality -- 1x/2x/4x durations, where
#                           t4-t2 == 2*(t2-t1). Three points, not two: two
#                           always fit a line because the implied template
#                           overhead absorbs any error.
#                         backend-parity -- MP4 and GIF of identical content
#                           must agree, exercising the subprocess and
#                           in-process decoders against each other.
#                         two-videos / video-before-image -- grid bookkeeping
#                           across several items when one has t_len > 1.
#                         heterogeneous-concurrency -- two clips with OPPOSITE
#                           color orders in flight at once; a crossed
#                           per-request offset returns the other's sequence.
#   legs_skipped  <= 0  on a runner WITH ffmpeg nothing should skip. Raise
#                       this only for a runner that deliberately lacks it.
#   control_held  = 1   the no-video control must not name the whole
#                       sequence, or the readings are not evidence.

[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.8-27B-NVFP4"
gate = "video-fidelity"
recipe = "qwen3.8/qwen3.8-27b-nvfp4-unsloth"
label = "Qwen3.8-27B dense (NVFP4, unsloth)"
# `default = false` since 2026-08-15: qwen3.6-27b owns this gate's required
# subject. Not a demotion of the checkpoint and not a lowered bar -- the
# thresholds below are unchanged and this entry is still measured whenever it
# is run explicitly. It is a choice of INSTRUMENT. `video-before-image` sits
# exactly at this checkpoint's capability edge: with the bright-green fixture
# it measured 13/13 on a manual serve (util 0.70, no drafts), 12/13 on this
# recipe as pinned -- "[red, green, blue]", one colour short -- and 0 colours
# with num_drafts=0, i.e. the leg's outcome moves with serve config while the
# geometry never does. qwen3.6-27b and qwen3.6-35b-a3b both pass it 13/13
# solidly. A gate whose required subject is one token away from failing
# reports on the model's edge, not on the video pipeline it exists to guard,
# and it would go red on PRs that changed nothing about video.
#
# This entry is also served by the AGENTIC recipe (thinking on, num_drafts 3,
# util 0.85), whose own metadata says "do not cross-use" it with the vision /
# tool profile. There is no vision-profile recipe for this checkpoint; adding
# one upstream in atlas-recipes is the real fix, after which this can take
# the default back with a measured run behind it.
default = false
status = "measured"
note = """\
REFERENCE RUN 2026-08-14, dgx-00, binary feat/video-support: PASS, 13/13 legs, 0 \
skipped, control held. Re-measured 2026-08-15 after the fixture fix: 13/13 on a manual \
vision-style serve, 12/13 under this entry's own agentic recipe (video-before-image \
one colour short) -- see the default = false comment above. group-proportionality \
1s=67 2s=116 4s=214 tokens -> 1/2/4 \
temporal groups at 49 tok/group with an 18-token template overhead; forward clip read \
"red, green, blue, yellow" and the reversed clip "Yellow, Blue, Green, Red"; MP4 and \
GIF both 214 prompt tokens; image+video 286; two-videos 437; video-before-image 288; \
video-in-history 249; C=1/2/4 all correct on one geometry. || THE REVERSED CLIP IS THE \
GATE. Identical geometry, identical prompt, frames in the opposite order -- no language \
prior gets that right twice, which is what makes the other legs evidence rather than \
plausible prose. || Requires ffmpeg; without it the MP4 legs SKIP and only the GIF \
subset runs (measured: PASS 5/5, 4 skipped)."""

# ★ RATCHETED 13 -> 14 on 2026-09-07, when #891 landed a fourteenth leg.
# This is a COUNT, not a measurement: every committed record before #891 reads
# exactly 13/13 and the first one after reads exactly 14/14. There is no
# variance for a tolerance to absorb, so the floor tracks the count exactly and
# a stale one guards nothing -- at min = 13 a later change that silently drops
# the new leg passes BOTH the scorer (13 asserted == 13 passed) and the floor
# (13 >= 13), and nobody hears about it. Count metrics are the easy half of
# threshold ratcheting and should never be left behind the instrument.
[benchmarks.metrics.legs_passed]
min = 14.0

[benchmarks.metrics.legs_skipped]
max = 0.0

[benchmarks.metrics.control_held]
min = 1.0

# --- kat-equality-gate ------------------------------------------------------
# The order-independence gate. REQUIRED (gate::coverage::REQUIRED) since
# 2026-09-10, promoted from PROMOTION_CANDIDATES once its excusal's stated
# precondition was met: "a measured run under --hermetic reaches zero
# divergences, and pin the sample count that was actually measured".
[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.8-27B-NVFP4"
gate = "kat-equality-gate"
recipe = "qwen3.8/qwen3.8-27b-nvfp4-unsloth-bfcl"
label = "Qwen3.8-27B dense (NVFP4, unsloth)"
# The only entry, so `default = true` is mandatory rather than a choice:
# bench.rs bails on zero defaults exactly as it does on two.
default = true
status = "measured"
note = """\
Does the same sample answer the same, whatever ran before it? One draw issued \
in two orders against ONE server, diffed byte-exact per `sample_id`. \
★ It rides the BFCL checkpoint and recipe DELIBERATELY: the gate replays \
BFCL's request body, so a different subject would answer about a workload \
nobody scores. ★ BUT DO NOT READ A GREEN HERE AS CERTIFYING THE BFCL GATE'S \
OWN CONDITIONS. This entry pins the CLOSED regime; the bfcl-subset entry pins \
no serve_overrides at all and is scored OPEN, which is the regime this gate \
measures at 36 divergences of 257. So a green says the BFCL workload is \
order-independent WHEN THE CHANNELS ARE CLOSED, and says nothing about the \
conditions the committed 84.22 / 84.12 were taken under. Closing that gap \
would mean pinning hermetic on bfcl-subset; the owner's decision (2026-09-13) \
is the opposite — bfcl-subset is certified by a partition of shards and stays OPEN, with \
the twelve partition-sensitive samples named in its own note — so the gap is \
documented rather than closed. That is \
also why `max_new_tokens` is pinned to BFCL's 1024 and not the driver's old \
512 — see the MAX_NEW_TOKENS commit. \
★ THE BOUNDS BELOW ARE INVARIANTS, NOT MEASURED PERFORMANCE. `diverged` and \
`unmeasured` are pinned at 0 because the gate's whole claim is that they ARE \
0 — the same shape as ssm-poison's `collapsed max = 0`, and the reason this \
gate may be promoted on a run it itself judges: no bar here was cut from that \
run. The one quantity taken from measurement is the sample count. \
★ THE SERVE PINS ARE THE SUBJECT, NOT CONFIGURATION. With the channels OPEN \
this gate reads 36 divergences of 257 on this checkpoint — measured 2026-09-10 \
on spark-43fa, 3878 s, identical 221, unmeasured 0, empty_replies 0 — and \
under the pinned regime it reads 0. That open-arm run is this gate's negative \
control, and it was run at THIS cap rather than inherited from the whole-draw \
measurement, because truncating the draw changes what ran before every \
surviving sample. \
★ Note 36, not the 12-of-995 that #936 records: those are different \
instruments. #936 compared the whole draw against its own four shards; this \
gate reverses the request order against ONE server, which is a stronger \
perturbation, so it sees more. Do not read the two numbers as a regression \
between them. \
The three keys are pinned together because --hermetic EXPANDS into the other \
two and check_record compares the two sets in both directions — a record \
carrying three against a baseline pinning one fails after the GPU hours are \
spent."""

[benchmarks.serve_overrides]
# All three, not just `hermetic`: bench.rs refuses an under-pinned hermetic
# baseline at PARSE time precisely so this cannot be discovered after a run.
# A record measured this way carries all three: see this gate's own committed
# record under .benchmarks/kat-equality-gate/, whose serve_overrides read
# {enable_prefix_caching: false, hermetic: true, mtp_gate: force}. (An earlier
# draft of this note cited an arm-D bfcl-subset record by sha; that record was
# measured on a box and never committed, so the citation named a file no
# reader could open. Cite something in the tree.)
hermetic = "true"
enable_prefix_caching = "false"
mtp_gate = "force"

[benchmarks.param_overrides]
# The instrument. Each of these is load-bearing for a bound below:
#   orders          — the `orders` metric pin is only a statement about the
#                     instrument if the param is pinned too.
#   sample_cap      — `samples` depends on it; unpinned, a --param sample_cap=50
#                     run would read 50/50 and look green with 20% of the draw.
#                     ★ 257 IS A SUBSET BOUNDARY, NOT A ROUND NUMBER. The draw
#                     concatenates in sorted subset order, and `truncate` keeps
#                     a prefix of it, so the cap decides WHICH SUBSETS are
#                     compared, deterministically — this is not sampling. The
#                     12 divergences measured on this checkpoint were 10
#                     live_irrelevance, 1 live_multiple and 1
#                     live_parallel_multiple; 257 is exactly the end of
#                     live_parallel_multiple, so it covers every subset in
#                     which the effect has ever been observed. What it gives
#                     up is 738 samples: live_simple (25) plus the whole
#                     non_live half (713 — multiple, parallel,
#                     parallel_multiple, simple_java, simple_javascript,
#                     simple_python). ★ Say live_simple out loud rather than
#                     calling the remainder "the non_live tail", which an
#                     earlier draft of this note did: live_simple is a `live`
#                     subset, so the cap DOES blind this gate to one subset of
#                     the category the effect lives in. Neither instrument has
#                     produced a live_simple divergence — not the 12 the shard
#                     split found, not the 36 the order reversal found — but
#                     that is a measured absence, not a structural one, and it
#                     is the first thing to re-check if a future divergence
#                     lands outside the capped prefix.
#                     The whole draw in two orders costs ~4.3 h (1990
#                     generations at the 7.9 s/generation this record measured)
#                     against this cap's ~1 h, on every PR, forever. State the
#                     cap when you report a green: the driver logs the same
#                     warning.
#   max_new_tokens  — BFCL's own budget. Divergence surface scales with it, and
#                     every committed bfcl-subset record was measured at 1024.
orders = "2"
sample_cap = "257"
max_new_tokens = "1024"

[benchmarks.metrics.orders]
# An exact pin: a run comparing fewer orders is not this instrument.
min = 2.0
max = 2.0

[benchmarks.metrics.samples]
# Pinned at what was ACTUALLY measured, per the promotion precondition.
min = 257.0
max = 257.0

[benchmarks.metrics.diverged]
# The invariant. Any divergence at all is the failure this gate exists for.
max = 0.0

[benchmarks.metrics.unmeasured]
# A timeout or transport error is NOT evidence that the orders agreed, so an
# unmeasured sample fails rather than being skipped.
max = 0.0

[benchmarks.metrics.empty_replies]
# Measured 0 of 257 on the open arm (2026-09-10). Pinned at 0 as a DEFINITION
# rather than as a performance bar: an empty canonical reply is not a KAT
# observation, and two empty replies agreeing is not evidence the orders
# agreed. `compare` already refuses a wholly-empty run as vacuous; this catches
# the PARTIAL case, where enough replies go empty to hollow the comparison out
# while the run still looks green.
max = 0.0
