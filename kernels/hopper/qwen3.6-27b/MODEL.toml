# Hopper target: kernel set inherited from gb10 via symlink; expected_absent
# NOT yet harvested on Hopper hardware (`spark serve --check-kernels`).
# Sampling, behavior and the 3.8 -> 3.6 source redirect retain GB10's settings.
# Only the architecture-declared optional W4A4 absence below is new here;
# no H100/H200 correctness or performance measurement is claimed.
[model]
name = "qwen3.6-27b"
hf_id = "Qwen/Qwen3.6-27B"
params = "27B"
active_params = "27B"
architecture = "Hybrid Attention + GDN + Dense FFN + MRoPE"

# Checkpoint-reference needles for kernel-target resolution (see
# atlas-kernels/src/resolve.rs). Required since qwen3.8-27b landed: that
# checkpoint's config.json is BIT-IDENTICAL to this one (same model_type
# "qwen3_5", same hidden_size 5120 — verified tensor-by-tensor 2026-08-14),
# so both targets declare exact (qwen3_5, 5120) and the tie is broken by
# matching these needles against the checkpoint's HF id / --model-name /
# model dir. "qwen3.5-27b" is claimed here ON PURPOSE: the standard
# multi-target build has always routed Kbenkhaled/Qwen3.5-27B-NVFP4 to this
# target (exact match beats qwen3.5-27b's wildcard — HANDOFF §9a, checked
# rc=0 2026-08-03), and that documented routing must survive the tie-break.
# qwen3.8 checkpoints do NOT match either needle and land on qwen3.8-27b.
match_names = ["qwen3.6-27b", "qwen3.5-27b"]

# Architecture mirrors the existing Qwen3.5-27B (hybrid GDN + full-attention,
# 4:1 pattern). Diff vs Qwen3.5-27B: vocab 152K → 248K, MRoPE-interleaved
# enabled by default in the upstream config (Qwen3.5-27B picked up MRoPE only
# in Kbenkhaled's NVFP4 re-upload; Qwen3.6-27B has it natively).
layers_total = 64
layers_full_attention = 16
layers_linear_attention = 48

hidden_dim = 5120
head_dim = 256
q_heads = 24
kv_heads = 4
intermediate_size = 17408

vocab_size = 248320
max_position_embeddings = 262144
rope_theta = 10000000
attn_output_gate = true
partial_rotary_factor = 0.25

# MRoPE (multi-modal rotary position embedding). Same configuration as
# Qwen3.6-35B-A3B's full-attention layers — interleaved 3-axis rope. Text-only
# serving keeps H/W position IDs at zero.
mrope_interleaved = true
mrope_section = [11, 11, 10]

mtp_layers = 0
vision = true

# Effective context for agentic tasks. Mirrors Qwen3.5-27B's empirical bound
# (Atlas's hybrid-GDN long-context behavior is consistent across 27B variants).
effective_context = 24576

# Recommended sampling defaults (carried over from Qwen3.5 family).
#
# Anti-repetition stack derived from commit 6c69d3f (which LIVE-BISECTED
# the OpenWebUI "3D chess game" degeneration: thinking-mode
# presence_penalty=0.0 — copied from the official Qwen3.6-27B card —
# drops the model into an announce/restart + verbatim-CSS loop on hard
# code-gen; QwenLM/Qwen3.6 #88/#115/#145).
#
# presence_penalty RETUNED 1.5 → 1.0 (2026-05-17, this branch). 6c69d3f
# used 1.5 on `thinking_coding` WITH XTC and code-routing; THIS branch's
# build_sampling routes enable_thinking → `thinking_text` (no
# thinking_coding selection) and has NO XTC port, and Open WebUI sends
# no sampling params so requests run at the preset default temp=0.6
# (stochastic, NOT greedy). Swept presence_penalty ∈ {0.3,0.5,0.8,1.0,
# 1.5} live at temp=0.6 on the chess prompt: 1.5 → premature EOS (574
# tok, 0 THREE. calls — the flat per-seen-token penalty boosts EOS once
# common code tokens are exhausted); 0.3 → complete but 3× wasteful
# announce/restart rewrites; 1.0 → single coherent complete impl
# (</script></body></html>, 17 THREE. calls, finish=stop, no loop). 1.0
# is the balance point between premature-termination and restart-spam
# at temp=0.6. DRY (multi-token phrase) + LZ (n-gram) carry the
# precision loop-breaking that presence (single-token) can't; the
# digit-normalized content-loop watchdog is the hard-stop safety net.
# tools stays 0.0 (grammar-constrained).
# 2026-05-24: empirical band-aid pattern (same as Qwen3.6-35B-A3B).
# temp=0.6 thinking, rep_pen=1.1 + dry_mult=0.5 to break agentic loops
# without the language-mixing/wandering side-effects of Qwen's
# presence_penalty=1.5 (validated on Qwen3.6-35B-A3B opencode session
# 2026-05-24). Top_p/top_k tracked from official model card.
# 2026-07-04: rep_penalty 1.1->1.0 + dry_multiplier 0.5->0.0 in ALL presets,
# syncing to the Qwen3.6-35B-A3B fix (2026-06-03, "rep_penalty is the
# code-corruptor"; vLLM serves penalty-free greedy). The 27B was the
# un-synced outlier: its tool-path 1.1/0.5 corrupted grammar-constrained
# tool-call args even at temp 0 (penalties are applied before the grammar
# mask, so they shift the argmax among legal JSON tokens — and tool-call
# JSON legitimately repeats " { , and key names), regressing BFCL ST-995
# non_live ~85->~76 vs the penalty-free 89.04 config.
[sampling.thinking_text]
temperature = 0.6
top_p = 0.95
top_k = 20
presence_penalty = 0.0
frequency_penalty = 0.0
repetition_penalty = 1.0
dry_multiplier = 0.0

[sampling.thinking_coding]
temperature = 0.6
top_p = 0.95
top_k = 20
presence_penalty = 0.0
frequency_penalty = 0.0
repetition_penalty = 1.0
dry_multiplier = 0.0

[sampling.non_thinking]
temperature = 0.7
top_p = 0.8
top_k = 20
presence_penalty = 0.0
frequency_penalty = 0.0
repetition_penalty = 1.0
dry_multiplier = 0.0

[sampling.tools]
temperature = 0.6
top_p = 0.95
top_k = 20
presence_penalty = 0.0
frequency_penalty = 0.0
repetition_penalty = 1.0
dry_multiplier = 0.0

[behavior]
# Content-loop watchdog enabled: dense Qwen3.6-27B has a known
# prose-attractor failure mode (2026-04-26 field reports) and the
# 2026-05-05 chess-game stress sweep confirmed it loops on long-code
# prompts (CSS block repetition at 13.3 t/s for ~3990 tokens). MoE
# A3B/A10B variants in the family don't need it.
enable_loop_watchdog = true
# Thinking on by default: dense Qwen3.6-27B is a reasoning model and
# clients (Open WebUI etc.) send plain requests with no enable_thinking
# flag. Without this the chat template injects an empty
# `<think>\n\n</think>` (thinking-off marker) so reasoning_content is
# always empty. An explicit per-request enable_thinking=false still wins.
thinking_default = true
# 2026-05-23 sweep: false → true (project-wide default after F1 removal).
thinking_in_tools = true
# 2026-05-23 sweep: 512 → 2048 (project-wide reasoning headroom bump).
max_thinking_budget = 2048
# `preserve_thinking` (tri-state) is deliberately UNSET here: the Qwen3.6
# checkpoint template's own default applies (strip historical `<think>`
# blocks before the last user query), which is byte-identical to the
# retired jinja-templates/qwen3_5.jinja override the MLPerf-edge reference
# prompts were measured with. Set `preserve_thinking = true/false` only to
# pin the Jinja variable for this target; per-request
# `chat_template_kwargs.preserve_thinking` still wins.
# 2026-07-24 chain-widening sweep: 1 → 3 (K=2 → K=4). The K=4 verify FFN on the
# 16 full-attention layers was falling through to the MMQ prefill GEMM (no n==4
# arm in multi_seq/ffn.rs) — the reason every earlier K>2 sweep regressed. With
# that arm added, the agentic ladder measures TPOT 38.49 (K=3) → 31.69 (K=4),
# and the full MLCommons e2e at K=4 gives wall 4104s / TPOT 32.60ms / BFCL 87.44
# / IoU 0.6231 (vs 4551.9s / 38.18ms at the old default). K=5 wins wall/tps by
# ~1.5-2.5% on the subset but has no full-e2e accuracy validation; revisit with
# its own e2e before moving further.
default_num_drafts = 3
# FP8 KV cache (enabled 2026-07-19): lever #2 — cp.async pipelining in
# inferspark_prefill_paged_fp8.cu hides KV-load latency behind QK^T compute.
# The old sync fp8->bf16 dequant path was 10.2% SLOWER than BF16 KV (435.8
# vs 480.2 tok/s @32k c=1 cold prefill). With cp.async pipelining (matching
# the BF16 kernel's 2-stage pattern), the FP8 kernel's load latency is now
# hidden, so FP8 KV is expected to be FASTER than BF16 KV (smaller DV75
# bandwidth, same compute epic). Resolution rule 3 in
# serve_phases/kv_cache.rs is now a no-op since the MODEL.toml default
# already matches the CLI default `--kv-cache-dtype fp8`.
default_kv_dtype = "fp8"

# Upstream Qwen/Qwen3.6-27B reports `model_type = "qwen3_5"` in config.json.
# Atlas's `parse_qwen3_5_config` (atlas-core/src/config.rs) rewrites this to
# `qwen3_6_moe` when MRoPE-interleaved is detected — same path Qwen3.5-27B
# already uses for the Kbenkhaled NVFP4 re-upload. The factory routes through
# the existing `Qwen35WeightLoader`; no new loader is needed because the
# weight key naming is unchanged from Qwen3.5.
[[model_types]]
model_type = "qwen3_5"
hidden_size = 5120

[[model_types]]
model_type = "qwen3_6_moe"
hidden_size = 5120

# DFlash block-diffusion speculative decoding pairing.
# Drafter (`z-lab/Qwen3.6-27B-DFlash`) is a separate 8-layer Qwen3-arch
# transformer that emits γ=16 tokens per step via bidirectional in-block
# attention. The 27B target's hidden_size (5120) determines the drafter's
# `fc` projection input dimension (5 * 5120 = 25600) — different from the
# 35B-A3B target's drafter (5 * 2048 = 10240) but the drafter checkpoint
# itself is the right shape per its own config.json.
[dflash]
draft_model = "z-lab/Qwen3.6-27B-DFlash"
gamma = 16
window_size = 4096
mask_token_id = 248070
# Verified 2026-05-03 against z-lab/Qwen3.6-27B-DFlash/config.json.
# `dflash_loader::parse_dflash_config` reads these from the drafter's
# config.json at runtime; this MODEL.toml field is the fallback default.
target_layer_ids = [1, 16, 31, 46, 61]

# ── Kernel lookups this model may legitimately fail to resolve ──
#
# TRANSITIONAL, and deliberately EMPTY. The boot audit fails closed on every
# unresolved lookup that is not listed here, so anything added is a lookup we
# have decided to keep issuing knowing it can never resolve.
#
# Prefer the other fix: gate the lookup on config so it is never ISSUED (see
# `qwen3_attention::init_arch_gates`, which is what removed the MLA /
# hyper-connection / CSA / HDIM-512 probes from every non-DeepSeek model). A
# lookup that is never issued leaves no row to explain.
#
# Shape — every entry needs a stated reason, and the build PANICS without one:
#
#   [expected_absent.<module>]
#   <kernel> = "why this model cannot use it"
#
# Populate from one boot: `spark serve <model> --check-kernels` prints the
# complete list for this target with each dispatch site, and exits non-zero
# while anything is unexplained.

[expected_absent.w4a4]
w4a4_gemm_mfast = """
This model's w4a4_gemm.cu defines only w4a4_gemm; the mfast variant exists solely
in nemotron-labs-3-puzzle-75b-a9b. Dispatch (qwen3_attention/init.rs) falls back
to w4a4_gemm, which is correct. This fallback predates the resolution gate and
was silent until it; it is declared here to unblock serving, NOT because the
fallback is known to be optimal. Whether porting mfast to this model is a win is
UNMEASURED - treat as a candidate, not a confirmed loss (routing m=9..32 into a
GEMV looked like the same shape and measured -14.4% at C=16). Remove this entry
if mfast is ported."""

# ptxas on sm_90a rejects the optional dense warp-level block-scale MMA.
# HARDWARE.toml disables it with ATLAS_NO_WARP_BLOCKSCALE_MMA; FP8 cells
# use the W8/BF16 path. Re-harvest all lookup results on real Hopper hardware.
w4a4_gemm = "Hopper sm_90a: mma with block scale is unsupported; the hardware guard excludes this optional NVFP4 entry point."

# Vendor MMQ symbols otherwise resolve to NO_DEVICE_CODE trap bodies.
[expected_absent.nvfp4_mmq]
atlas_nvfp4_mmq128_nc = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
atlas_nvfp4_mmq128_wc = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
atlas_nvfp4_mmq16_nc = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
atlas_nvfp4_mmq16_wc = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
atlas_nvfp4_mmq32_nc = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
atlas_nvfp4_mmq32_wc = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
atlas_nvfp4_mmq64_nc = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
atlas_nvfp4_mmq64_wc = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
atlas_nvfp4_quantize_bf16 = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
atlas_nvfp4_repack = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
atlas_nvfp4_scale_bf16 = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
atlas_nvfp4_silu_mul_scaled = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
atlas_nvfp4_silu_mul_quant = "Hopper sm_90a lacks the vendor BLACKWELL_MMA_AVAILABLE warp FP4 path (SM 12.x only); absent handles retain W4A16 prefill and transposed weights."
